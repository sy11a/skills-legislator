# v22 — the baseline and the linter, measured on the instrument BL-062 fixed

**Result: 201/201 corpus, idempotency ×3 zero diff, one law generation, one
fix round.** Model floor **`sonnet`**, same as v21. Every number below is
`<measured>/<total>` under BL-062's arithmetic: nothing in this edition was
scored against an absent artifact.

Law generation `v22-e277e4c` for the final pass. The first corpus pass ran at
`121ff91..97e27ed` (VERSION already 22) and went 200/201; fix round 1
(`e277e4c`, a `references/restructure.md` amendment) closed the one red, and
the full suite was re-run from scratch on the amended generation — the
201/201 is a single pass over one law, not a patchwork of passes.

## Harness

| | |
|---|---|
| Runner profile | `claude` |
| Claude Code | 2.1.241 |
| Model | `sonnet` |
| Final run id | `20260826-0212`, law commit `e277e4c` |
| opencode profile | **not run** — frozen since 2026-08-24 (BL-054) |

## The baseline, and the confound it was designed around

Per POLICY §5's grader-change rule — its first use — the baseline was NOT
taken from the `v21` tag: BL-062 changed the grader without changing the law,
so the tag's worktree would have resurrected the old instrument. The baseline
ran on **master at BL-062** (v21 law, current grader), run `20260825-2205`,
same harness and model:

| Scenario | v21 baseline | v22 |
|---|---|---|
| `upgrade` | 21/21 | 21/21 |
| `legacy-migration` | 27/27 | 27/27 |
| `legacy-migration-agents-first` | 22/22 | 22/22 |
| `fresh-scaffold-dotnet` | 19/19 | 19/19 |
| `audit` (rotted-layer) | 44/44 | **45/45** |
| `restructure` | 38/38 | 38/38 |
| `upgrade-drop-stack` | 16/16 | 16/16 |
| `case-practice` | 7/7 | **8/8** |
| `audit-engine-absent` | 5/5 | 5/5 |
| **Total** | **199/199** | **201/201** |
| Idempotency (fresh, upgrade, restructure) | zero, zero, **DIFF** | zero, zero, zero |

The two new asserts: `audit` gains the BL-057 absent-marker (the report must
not name the quoted-token document), `case-practice` gains
`delivered_engine_sdd_lint_clean` (the delivered engine's own lint judges the
agent's case tree). Both were shown red against the v21 law before their fix:
the marker by a live targeted audit run on a v21-law worktree — the v21 agent
flagged `templating-notes.md` as a Critical and proposed exactly the two
remedies BL-057 rejects — and the lint assert at `exit=2: usage:
{anchors|okf-debt}` on the baseline's delivered engine. Evidence:
`docs/cases/BL-057-quoted-tokens/red-evidence.md`.

## Defect chronicle

### Law (1)

| Defect | How the corpus surfaced it | What it forced |
|---|---|---|
| **Restructure's `fix` inherited Step 4's ask-the-user derivation.** `fix` says unresolved tokens are "filled per SKILL.md Step 4's derivation rules"; Step 4's rule for `{{PROJECT_OVERVIEW}}` is *ask the user*. A headless run cannot, so one agent lawfully left the token unfilled and wrote the diagnosis into its own report ("this run cannot invent it") while another lawfully filled it from the repo — two lawful outcomes, POLICY §6's nondeterminism class. | Twice, and the two sightings are the instrument's before/after: on the **v21 baseline** run 1 skipped the fill, the porcelain-only grader scored the tree **38/38**, run 2 filled it, and only the idempotency diff exposed the pair. On the **v22 first pass** the same skip was caught **on run 1** — 37/38, naming the file — because the whole-tree token scan landed in between. | Fix round 1 (`e277e4c`): where Step 4 derives by asking the user, restructure derives from the repo's own content and proposes the value in the plan item itself — approving the item approves the text (the glossary-seeding precedent one clause over). Rerun: 38/38, idempotency zero diff. |

### Grader (1, found by the baseline — fixed inside this cycle before the corpus ran)

| Defect | Evidence | What it forced |
|---|---|---|
| **`no_unresolved_placeholders` scanned the porcelain set, not the tree.** A planted token in a file the run never touched was invisible: the assert reported "no stray {{TOKEN}}s" over a tree that carried one. v17's invisible-token defect alive one layer up — the assert v17 added to close it was itself blind to the untouched file. | The baseline's restructure run 1, re-graded on the reconstructed run-1 state: 38/38 under the old scan, 35/38 under the whole-tree scan, naming `docs/okf/overview-draft.md`. | The scan now covers tracked files united with the porcelain set, under the same quotation rule as check 2 and sdd-lint — three scanners, one definition of prose. |

### Harness (1) and model (0)

The first v22 corpus attempt (2026-08-26 00:02) lost eight scenarios in six
minutes to the account's session limit — every agent returned "You've hit
your session limit" as a one-turn success. The orchestrator classified it
correctly: `corpus had failures — idempotency stage skipped`, exit non-zero —
v21's quota incident wearing the same clothes, but this time the instrument
saw it (BL-058, BL-062). No false green; the run was repeated after reset.
The second attempt then stalled overnight on the same limit and resumed by
itself once the window reset — the resume ladder's cost, not a defect.

Nothing was filed as model-class. The class's record stands at 0 for 3.

## Flaky watch

The BL-057 red run (a v21-law audit, one pass) also showed seven adjacency
report-markers (`orphan-docs] docs/okf/orphan-notes.md` …) failing on a
report whose findings were all present but wrapped paths in backticks —
`[orphan-docs] ` + backticked path defeats the substring. The v22 corpus
did not reproduce it (both audit runs wrote plain paths), so nothing was
changed; recorded here because the class is the same one defect 15's
order-independent markers fixed in v18, and the next edition that sees these
asserts flake should reach for that repair, not re-diagnose.

## Model floor

`sonnet` — the whole suite at 100% in one pass, twice this cycle (the
baseline on the v21 law, the final run on v22). Not re-derived downward;
haiku was not attempted (v17's measurement stands: short on judgement-heavy
modes).

## Idempotency

Zero diff ×3 (fresh-scaffold, upgrade, restructure) on the final generation.
The baseline's restructure DIFF is the law defect above, not noise: the
second run filled what the first left undone — POLICY §6's definition of a
first run that left work undone, and the fix landed in the law, not in the
diff.
