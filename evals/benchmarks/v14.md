# Eval results — v14 (2026-08-05, constitution VERSION 13 → 14)

Change under test: **cross-harness constitution** — the legislator now serves
both Claude Code and opencode from one source of truth.

- `AGENTS.md` becomes the canonical constitution; `CLAUDE.md` is a symlink →
  `AGENTS.md` (Claude Code reads `CLAUDE.md`, opencode reads `AGENTS.md`).
  Template renamed `CLAUDE.md.tpl` → `AGENTS.md.tpl`.
- **NEW owned wiring file `opencode.json`** at repo root (byte-identical,
  refreshed every run, write-guarded). opencode auto-loads it and uses its
  `instructions` globs to pull the owned rules + codebase-map + glossary +
  `.claude/rules/` into context — the bridge for opencode not expanding
  `@imports`. Source: `assets/templates/opencode.json.tpl` (no placeholders).
- Step 3 owned-file application + manifest `ownedFiles` now include
  `opencode.json`; the write-guards (`guard_owned_files.py` AND the Stage-A
  opencode plugin `legislator-guard.ts`) block hand-edits to both
  `docs/ai/rules/**` and `opencode.json`.
- SKILL.md "Ensure the v14 file model (every mode, including upgrade)" clause:
  any run on a pre-v14 repo (real `CLAUDE.md`, no `AGENTS.md`) canonicalizes
  it — rename + symlink + opencode.json — so upgrades and restructure-heals
  migrate the file model, not just refresh rules.
- Audit check 9 inverts: `AGENTS.md` is canonical (never foreign), `CLAUDE.md`
  is its symlink (never foreign); a non-symlink `CLAUDE.md` beside an
  `AGENTS.md` in a current-manifest repo is the Warning. Skill-discovery
  generalized to `~/.claude/skills/`, `~/.agents/skills/`,
  `~/.config/opencode/skills/`.

| Scenario | Pass | v13 pass |
|---|---|---|
| fresh-scaffold-dotnet | 18/18 | 17/17 |
| legacy-migration | 26/26 | 25/25 |
| upgrade (v13→v14, model migration) | 18/18 | 17/17 |
| audit (15 defects, staleness now v14) | 27/27 | 27/27 |
| restructure | 28/28 | 28/28 |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 |
| idempotency:upgrade | 1/1 zero diff | 1/1 |
| idempotency:restructure | 1/1 zero diff | 1/1 |

Total 120/120 (v13: 117/117). +3 assertions: the `v14_model_agents_canonical_
claude_symlink` check in common_checks (fresh + migration + upgrade) verifies
AGENTS.md exists as a real file AND CLAUDE.md is a symlink resolving to it.
opencode.json ownership is covered by the existing `manifest_ownedFiles_exact
_sorted` + `owned_files_verbatim` (now 14 owned entries incl. `opencode.json`).
The `agents_md_imports_*` checks replace the old `claude_md_imports_*` (same
semantics, canonical filename). `grade_upgrade`'s protected-untouched list
drops `CLAUDE.md` (now a managed symlink that legitimately changes in a
v13→v14 model migration). Static checks: all passing (incl. the new
`opencode.json.tpl` well-formedness check and the AGENTS.md.tpl core-import
check). Hook checks: 31/31 (added opencode.json + package.json guard cases).
opencode-plugin checks: 14/14 (Stage A).

Notes — full disclosure:

1. **Clean first-pass e2e — no re-runs.** All five scenarios graded green on
   their first agent run, and all three idempotency re-runs were zero-diff on
   the first try. The v14 file model (AGENTS.md + symlink + opencode.json)
   was applied correctly by fresh, migration, and upgrade agents without
   prompting iteration — the strongest signal that the SKILL.md procedure +
   migration.md §0 are unambiguous. The audit agent correctly treated the
   rotted fixture's real-`CLAUDE.md`/no-`AGENTS.md` state as legitimate
   pre-v14 (covered by the v14 staleness finding), not as a foreign-structure
   false positive — exactly the check-9 inversion's intent.
2. **Token / wall-time figures not instrumented this run.** The harness here
   did not capture per-scenario token or wall-time telemetry, so the
   cost columns are omitted rather than estimated. v13's cost growth was
   flagged as worth watching; v14 adds the opencode.json copy + (in
   migration/upgrade) one rename+symlink — a small per-run increment. Capture
   proper figures at the next benchmark if cost-tracking matters for a
   decision.
3. **`opencode.json` ownership model:** owned, byte-identical, refreshed
   every run. Project-specific opencode needs (permissions, mcp) are
   expected to live in the global `~/.config/opencode/opencode.jsonc` or a
   future project-owned override — NOT in this owned file (re-running
   `/legislator` would re-assert the canonical bytes). If a real product repo
   later needs project-level opencode keys, switch that repo to
   "project-owned opencode.json with the `instructions` block proposed in
   Step 7" (mirrors how AGENTS.md is already project-owned).
4. **Next: fleet migration.** The four real legislated repos
   (RKruiterApi/Agent, CareerPlatform, RKruiter_TestClient) move v13→v14 via
   `/legislator` upgrade — pilot RKruiterApi first, review the diff, then
   fleet.
