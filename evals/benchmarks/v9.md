# Eval results — v9 (2026-07-10, constitution VERSION 8 → 9)

Change under test: BL-016 stray rulebooks + .NET refactoring law. (a) Second
constitution-content change: generic refactoring law absorbed into
concern-named files — 5 async/cleanliness bullets in
`stacks/dotnet/coding-standards.md`, 2 DI bullets in
`stacks/dotnet/architecture.md`, new `stacks/dotnet/data-access.md` (6 EF
bullets). (b) Stray-rulebook feature: audit check 12 (`stray-rulebooks`,
Warning) flags law-shaped rule/checklist docs no session loads; harvest
scans them (the one carve-out to the `docs/superpowers/**` exclusion);
restructure merges their law into `.claude/rules/<topic>.md` and removes
the file. BL-015 riders shipped (harvest test 2 counts owned-law
contradictions as covered + grader lock; check 11 Info note when the dir is
absent on a current manifest; restructure.md §1 "Narrative AI rules prose";
check 10(b) exempts `.claude/rules/**`).

| Scenario | Pass | v8 pass | Tokens (v8) | Wall time (v8) |
|---|---|---|---|---|
| fresh-scaffold-dotnet | 14/14 | 14/14 | 64,875 (71,986) | 137 s (142 s) |
| legacy-migration | 22/22 | 22/22 | 91,495 (118,536) | 399 s (455 s) |
| upgrade (v8→v9) | 13/13 | 13/13 | 71,230 (75,350) | 214 s (169 s) |
| audit (12 defects, +4 assertions) | 22/22 | 18/18 | 72,099 (69,378) | 209 s (202 s) |
| restructure (+4 assertions) | 21/21 | 17/17 | 94,467 (89,729) | 377 s (364 s) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 49,920 (54,949) | 57 s (82 s) |
| idempotency:upgrade | 1/1 zero diff | 1/1 | 64,464 (53,584) | 190 s (92 s) |
| idempotency:restructure | 1/1 zero diff | 1/1 | 71,279 (89,104) | 173 s (323 s) |

Total 95/95 (v8: 87/87; +8 assertions, all new-feature coverage). Static
checks: all passing (stack-filename allow-list gained `data-access.md`).
Hook checks: 28/28.

Notes:

1. **First fully clean single-pass benchmark** — five prior benchmarks each
   caught at least one drift mid-run; this one needed no mid-benchmark
   fixes and no re-runs. Every scenario was graded against the exact
   shipped bytes.
2. **Check 12 fired precisely once, on the planted stray rulebook**, and
   the harvest handled its content correctly on both sides: the generic
   reversible-migrations line was proposed as a candidate while the
   project-specific PdfRenderer line was not (both locked by
   candidates-section-scoped markers, a new grader mechanism). The BL-015
   rider-1 lock also held: the owned-law-contradicting `.claude/rules/
   journal.md` line appeared only as a check-11 finding, never as a
   candidate.
3. **Restructure routed the stray rulebook end-to-end**: merged both law
   lines into `.claude/rules/code-review.md` (a new topic file it chose
   itself), removed the source, and the fidelity greps confirmed zero
   content loss. The two planted conflicts stayed decision-gated and open;
   the second run re-surfaced them with zero writes and `Fidelity: verified
   (0 lines tracked)` — the no-op wording the fixture expects.
4. **The v8→v9 upgrade path works end-to-end**: fixture generated from
   current source withheld `project-rules.md` (still alphabetically last);
   new rule copied in, retired rule deleted, manifest to v9, keep list
   carried and extended per prompt, zero spurious diffs on the second run.
   Note the generated fixture ships current-source bytes for the *edited*
   dotnet files, so the upgrade scenario exercises the added/removed paths,
   not re-copying changed bytes — same methodology as every prior version.
5. Token costs: migration −23% and idempotency:restructure −46% (cheaper
   than v8 despite more law on disk); audit +4% with four more assertions;
   idempotency:upgrade +20% (64.5k) — within run-to-run noise for a 1/1
   scenario. Nothing trending the wrong way; pass rate 95/95.
