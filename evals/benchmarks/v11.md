# Eval results — v11 (2026-07-12, constitution VERSION 10 → 11)

Change under test: BL-020 glossary vitality — three levers against dead
glossaries. (1) `{{GLOSSARY_TABLE}}` derived at legislation (5–15 confirmed
terms from the repo's own domain) instead of an intentionally empty table;
(2) `core/okf.md`'s mandatory completion checklist gains a glossary item;
(3) audit check 13 (`glossary-vitality`, Warning) + restructure `fix` heal
via the Step 4 derivation rule. BL-019 riders shipped: service-locator
bullet gains the `IServiceScopeFactory`-scope carve-out; pair-development
"never cut a new task branch *from main*…" + "parked" defined; import-line
grader scoped to the Needs-your-review block; check 9 elevates to Warning
for predominantly law-shaped foreign configs; conventional-doc exemption
covers `docs/` and `.github/` variants. Mid-benchmark, check 11 was also
extended to scan CLAUDE.md prose for owned-rule contradictions (see note 3).

| Scenario | Pass | v10 pass | Tokens (v10) | Wall time (v10) |
|---|---|---|---|---|
| fresh-scaffold-dotnet (re-run, note 1) | 15/15 | 14/14 | 71,631 (62,418) | n/m (161 s) |
| legacy-migration (re-run, notes 1, 4) | 23/23 | 22/22 | 82,853 (86,571) | 369 s (317 s) |
| upgrade (v10→v11) | 16/16 | 16/16 | 81,297 (79,084) | 350 s (292 s) |
| audit (13 defects, regraded, note 2) | 23/23 | 22/22 | 72,776 (66,116) | 210 s (186 s) |
| restructure (re-run, note 3) | 23/23 | 21/21 | 96,991 (85,085) | 528 s (357 s) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 52,577 (53,510) | 72 s (71 s) |
| idempotency:upgrade | 1/1 zero diff | 1/1 | 56,567 (59,516) | 119 s (123 s) |
| idempotency:restructure | 1/1 zero diff | 1/1 | 71,005 (81,368) | 200 s (270 s) |

Total 103/103 (v10: 98/98; +5 assertions: glossary seed ×2, glossary
defect marker, glossary heal ×2). Static checks: all passing. Hook checks:
28/28.

Notes — three mid-benchmark catches, all fixed and re-run honestly:

1. **Ambiguous-bait class, third occurrence (v7.4 precedent):** the first
   fresh and migration runs seeded ZERO glossary rows — correctly, per the
   new rule's "never invent domain concepts the repo doesn't evidence":
   both fixtures were domain-blank (a bare `dotnet new webapi`; a legacy
   CLAUDE.md of pure rules/conventions). Fixture fix, not skill fix: the
   fresh README gained explicit domain vocabulary (order/line item/
   customer/status lifecycle/unit price snapshot) and the legacy CLAUDE.md
   gained a "Domain notes" section (invoice, dunning run, PSP) — which
   also live-exercises migration §1's new definition-carve (the run turned
   those definitions into glossary rows). Re-runs: fresh 15/15 in v11b
   (5 terms seeded), migration 23/23 in v11d (3 terms seeded).
2. **Grader precision bug (measurement, not behavior):** the candidates-
   section regex didn't terminate at the pinned `Clean checks:` line, so an
   agent's trailing notes paragraph (quoting the wkhtmltopdf line while
   explaining why it was NOT proposed) was swallowed into the section and
   tripped the absent marker (22/23). The section by definition ends at
   `Clean checks:`; regex fixed in both graders and the SAME audit run
   regraded: 23/23. No agent behavior changed.
3. **Check 11 gap exposed by the restructure run:** the first run missed
   surfacing the planted CLAUDE.md "we do not maintain CHANGELOG.md"
   conflict as a decision item (22/23) — no audit check pinned
   CLAUDE.md-prose-vs-owned-rule conflicts; v9/v10 runs had caught it by
   incidental judgment. Fixed by extending check 11 to scan CLAUDE.md's
   project prose (same finding shape, `CLAUDE.md` as path); restructure
   re-run in v11d: 23/23, both conflicts decision-gated, and the
   idempotency run's diagnose now reports the conflict under the pinned
   `[project-rules] CLAUDE.md` slug. Fourth judgment-to-pinned-text
   conversion across the benchmarks.
4. Bookkeeping: one migration attempt (v11c workspace) was lost to an
   infrastructure stall (agent watchdog, no grading artifact) — rerun in
   v11d; the first-run fresh and restructure wall times were inflated by
   the same stall window, so fresh's is marked n/m and restructure's 528 s
   should be read with that caveat. Audit/fresh/upgrade were graded
   against pre-note-3 bytes: assertion-neutral (their fixtures plant no
   CLAUDE.md conflict; the extension only adds findings those scenarios
   never test). Glossary seeding is create-once: all three idempotency
   passes re-serialized populated glossaries byte-identically.
