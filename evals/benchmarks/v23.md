# v23 — the audit engine, the emitter, and the case-shape lints

**Result: 205/205 corpus, idempotency ×3 zero diff, mutation pass 205/205
killed (0 survived / 0 uncovered / 0 duplicates), model floor `sonnet`**
(unchanged since v17). Every number is `<measured>/<total>` under BL-062's
arithmetic; every assert was killed by its mutation under POLICY §1c.

Law generations: the first corpus pass ran at `1600bee` (VERSION already
23); fix rounds landed `de05ee0` (two engine defects, one grader marker),
`2ae9e38` (grader, escalation semantics) and `dae7cb2` (law, the debris
classification pin). Final generation for the record: **`dae7cb2`**.

## Harness

| | |
|---|---|
| Runner profile | `claude` |
| Claude Code | 2.1.241 |
| Model | `sonnet` |
| Workspace | `/tmp/legislator-eval-v23` |
| opencode profile | **not run** — frozen since 2026-08-24 (BL-054) |

## Baseline and confounds (named, per §9)

v23 changes **law and grader together** (the audit rewire changes what the
agent does *and* what the grader demands), so no baseline run could isolate
the law — the reference is the v22 benchmark on its own instrument
(`benchmarks/v22.md`, 201/201, same harness/model). Confounds vs v22:
four new asserts (audit +3, engine-absent +1), two matcher rewrites
(`conflict_surfaced_as_decision`, `audit_mechanical_findings_match_engine`),
and the audit scenarios now measure an engine-printed report. Per-scenario
numbers are comparable outside the audit family; the audit family is a new
instrument.

| Scenario | v22 | v23 |
|---|---|---|
| `upgrade` | 21/21 | 21/21 |
| `legacy-migration` | 27/27 | 27/27 |
| `legacy-migration-agents-first` | 22/22 | 22/22 |
| `fresh-scaffold-dotnet` | 19/19 | 19/19 (now incl. `.gitattributes`) |
| `audit` (rotted-layer) | 45/45 | **48/48** |
| `restructure` | 38/38 | 38/38 |
| `upgrade-drop-stack` | 16/16 | 16/16 |
| `case-practice` | 8/8 | 8/8 |
| `audit-engine-absent` | 5/5 | **6/6** |
| **Total** | **201/201** | **205/205** |
| Idempotency (fresh, upgrade, restructure) | zero ×3 | zero ×3 |
| Mutation pass | 201/201 killed | **205/205 killed, 0/0/0** |

**The one-law-generation note.** The final law fix (`dae7cb2`) amends one
sentence inside SKILL.md's *Restructure* section (the debris
classification). Only `restructure` executes that section, and it — plus
its idempotency pass — re-ran under the final generation. Every other
scenario's law surface (the mode sections it reads, and every delivered
owned file) is byte-identical between its run generation and `dae7cb2`, so
those runs stand without a blanket re-run — stated here so a reader can
disagree with the reasoning rather than discover it. (v22 re-ran the full
suite after its restructure amendment; this edition claims the narrower
surface argument explicitly, trading a blanket re-run for a named
assumption, per the owner's verification economics.)

## The red-first record for the new asserts

`audit_report_carries_engine_stamp` (both audit scenarios),
`audit_mechanical_findings_match_engine` (16 engine lines absent), and the
`.gitattributes` scaffold expectation were shown red against the v22
workspace artifacts — zero agents, the artifacts already existed.
`model_findings_in_pinned_sections` is the recorded deviation: the v22
model already placed those lines lawfully, so its red evidence is its
mutation (`move-out-of-section`), which this pass shows killing it.

## Defect chronicle

### Law (4)

| Defect | How the corpus surfaced it | What it forced |
|---|---|---|
| **Engine check 14 scanned `prose_only` text, which strips inline code — every backticked skill name vanished before the regex ran.** The check reported clean; the restructure agent then *correctly deferred to the engine* and routed nothing, going red for obeying the instrument. | `report names 'skill-bindings]'` and `'made-up-skill'` absent (audit); `skill_binding_for_the_team_not_a_plan_item` (restructure). | `de05ee0` — check 14 scans raw text (there, the backticks ARE the signal), red-first unit test. One fix closed three reds across two scenarios. |
| **Engine journal-recency read only full-stem filename dates.** The fixture's `2026-01-15-setup.md` and content-dated entries — both lawful per "from filenames or entry content" — read as "no dated entries". | `report names '2026-01-15'` absent (audit). | `de05ee0` — date-prefix on filenames plus a content-date fallback, red-first. |
| **The scratch-debris classification permitted two outcomes** — foreign structures carry `move-or-merge` authority, while inventing project content is forbidden; `.superpowers/` debris sits in both readings. Run 1 left it a `[decision]`; the idempotency second pass *moved it into an invented journal entry* — POLICY §6's exact "open decision reclassifies into an applied write" class. | `second_run_zero_diff` (restructure idem): run 2 deleted the debris and created `docs/journal/2026-07-01-task-9.md`. | `dae7cb2` — the Restructure scope pins it: agent-tooling scratch debris is never a `move`/`merge` source; its only lawful plan item is a `[decision]`. Restructure + idem re-run green under the pin. |
| **`case_is_converged` matched the marker as a substring anywhere in the case** — a spec *quoting* "✅ Converged" in prose (BL-065's own spec did, explaining a dropped lint) silently converged its case, exempting it from every shape lint; the battery read clean while the open spec lacked its Boundary and Clarifications. Found post-benchmark by member-#0 self-audit, not by the corpus — no fixture quotes the marker, so the corpus surface is unaffected (the same named-assumption discipline as above). | member #0's own battery: `sdd-lint` silent on a spec the new lints should have flagged. | post-benchmark fix, red-first: the marker closes a case only when a line *starts* with it (bold/date decoration lawful — the historical `✅ Converged.` and `**✅ Converged** 2026-08-24.` forms still close); a mid-sentence quotation never does. The BL-057 lesson's third appearance: a quotation is never the artifact. |

### Grader (2)

| Defect | How it surfaced | What it forced |
|---|---|---|
| **`conflict_surfaced_as_decision` graded typography, twice.** Round 1: the marker demanded the fixture sentence's exact casing; the agent lawfully lowercased it inside its item. Round 2 (after a case-insensitive patch): the next agent paraphrased the sentence entirely — still lawful, both sides named. | red on a report whose item 15/16 named `AGENTS.md ↔ core/changelog.md` with the conflict stated. | `de05ee0` then `dae7cb2` — the matcher is shape-based now: a `[decision]` line pairing the entry document with the owned changelog rule; the mutation retargeted to drop that line. The v18 lesson, re-learned at full price. |
| **`audit_mechanical_findings_match_engine` did not model the `escalates` channel.** An engine Info line lawfully superseded by a model escalation (`.cursorrules`, check 9) read as a missing line. | red on the re-run's otherwise-lawful report. | `2ae9e38` — an absent engine line is forgiven only when its path is still named by another finding line. |

### Harness (2 — both operator-induced, recorded against the operator)

| Incident | Effect | What it forced |
|---|---|---|
| A `bash -x` duplicate invocation, launched to diagnose what looked like an instant runner death (it was actually the 85-minute run finishing red), was killed mid-smoke — leaving `upgrade` a half-run corpse, and exposing that **every invocation wipes every scenario's `grading.json`**, including scenarios it never touches. | one scenario re-run; run-1 records survived only in `grade-history.jsonl` (which is why that file exists). | operational rule for the chronicle: never launch a second invocation against a live workspace; regrade restores records cheaply. |
| **The mutation pass raced a concurrently-started runner invocation** — the runner's invocation-start cleanup broke the Reverter's substrate mid-pass, silently losing one reverted line (`conflict` fixture sentence) from the restructure repo. | restructure regraded 36/38 an hour after being 38/38; two mutation summaries produced on a moved substrate were discarded. | one scenario + idem re-run; a workspace lock is filed as a hardening backlog item. The authoritative mutation pass ran isolated, after all agents. |

### Model (0)

The class stays 0-for-3 all-time: every red this cycle traced to law,
grader, or harness.

## What this edition shipped

BL-065: nine case-shape lint families in `sdd-lint` (member #0 lints
clean, and the lints' first live catch was this repo's own unclosed
BL-060 marker). BL-066: the engine `audit` job (15 mechanical checks) and
the report emitter with the model-findings channel — the audit report is
now an engine print with pinned model slots, per BL-049's contract.
Riders: engine `encoding="utf-8"` everywhere; `okf-debt`/audit fail loud
without git; the `.gitattributes` scaffold row; `tools/proc.py` replacing
the runner's Linux-isms (this benchmark's ~10 agent invocations are its
live verification).
