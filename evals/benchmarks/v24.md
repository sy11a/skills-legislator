# v24 — the engine writes the owned layer, and prints the Step-7 report

**Result: 214/214 corpus, idempotency ×3 zero diff, mutation pass 214/214
killed (0 survived / 0 uncovered / 0 duplicates), model floor `sonnet`**
(unchanged since v17). Every number is `<measured>/<total>` under BL-062's
arithmetic; every assert was killed by its mutation under POLICY §1c.

Law generation: one — the corpus ran at `7e36d99` (VERSION 24) and went
green in a single pass; the only fix this cycle was a grader defect found by
the mutation pass (chronicle below). Final generation for the record:
**`7e36d99`** plus that grader fix.

## Harness

| | |
|---|---|
| Runner profile | `claude` |
| Claude Code | 2.1.251 |
| Model | `sonnet` |
| Workspace | `/tmp/legislator-eval-v24` |
| opencode profile | **not run** — frozen since 2026-08-24 (BL-054) |

## Baseline and confounds (named, per §9)

v24 changes **law and grader together** (Steps 1/3/6/7 become engine
invocations; the grader gains the stamp, the re-print match and the scaffold
probe), so no baseline run could isolate the law — the reference is the v23
benchmark on its own instrument (`benchmarks/v23.md`, 205/205, same
harness/model). Confounds vs v23: nine new asserts (upgrade +4, fresh +2,
migration +1, agents-first +1, drop-stack +1); the harness ground rule that
named `cp` now names the engine; the fresh-scaffold report is a persisted
artifact for the first time; Claude Code 2.1.241 → 2.1.251.

| Scenario | v23 | v24 |
|---|---|---|
| `upgrade` | 21/21 | **25/25** (stamp, engine re-print match, Created/Deleted lines) |
| `legacy-migration` | 27/27 | **28/28** (stamp) |
| `legacy-migration-agents-first` | 22/22 | **23/23** (stamp) |
| `fresh-scaffold-dotnet` | 19/19 | **21/21** (report probe, stamp) |
| `audit` (rotted-layer) | 48/48 | 48/48 |
| `restructure` | 38/38 | 38/38 |
| `upgrade-drop-stack` | 16/16 | **17/17** (stamp) |
| `case-practice` | 8/8 | 8/8 |
| `audit-engine-absent` | 6/6 | 6/6 |
| **Total** | **205/205** | **214/214** |
| Idempotency (fresh, upgrade, restructure) | zero ×3 | zero ×3 |
| Mutation pass | 205/205 killed | **214/214 killed, 0/0/0** |

## Cost and time per scenario (one run each, `[result]` lines)

| Scenario | v23 turns / s / $ | v24 turns / s / $ |
|---|---|---|
| `upgrade` | 41 / 262 / 0.82 | 37 / 171 / 0.74 |
| `legacy-migration` | 47 / 314 / 1.03 | 41 / 250 / 1.02 |
| `legacy-migration-agents-first` | — | 47 / 284 / 1.01 |
| `fresh-scaffold-dotnet` | 44 / 162 / 0.65 | 41 / 168 / 0.66 |
| `upgrade-drop-stack` | — | 33 / 193 / 0.80 |
| `audit` (rotted-layer) | 18 / 249 / 0.71 | 25 / 347 / 0.97 |
| `audit-engine-absent` | — | 14 / 104 / 0.39 |
| `restructure` | 63 / 769 / 2.62 | 55 / 564 / 1.90 |
| `case-practice` | — | 176 / 1367 / 6.53 |

Where Step 3 and Step 7 carried the run (upgrade, migration, restructure's
`heal` and final report) the run is 20–35% shorter and cheaper: the model no
longer issues a dozen `cp`s, computes `ownedFiles`, diffs for deletions,
serializes the manifest, re-diffs in Step 6 and composes the report line by
line — it invokes three jobs and writes one findings file. Fresh scaffold is
unchanged (Step 4's placeholder derivation dominates it and stays the
model's). Audit is slower this run (+40%); the audit job did not change in
v24, so this reads as single-run variance, not a signal — one data point,
recorded, not concluded.

## The red-first record for the new asserts

Against a copy of the v23 workspace (no agents): `report_carries_engine_stamp`
red in all four report scenarios, `scaffold_report_saved` red (absent),
`report_mechanical_lines_match_engine` **unmeasured** — no run record exists
under v23, by construction — and `report_created_lists_new_rules` /
`report_deleted_lists_retired_rule` green, exactly as the spec predicted
(the v23 model listed them lawfully); their falsifiability evidence is their
`remove-lines` mutation, which this pass shows killing them. Engine units:
36 shown FAIL (unknown job, exit 2) before the jobs existed, then green;
one more added red-first for the emitter defect the manual run found.

## Defect chronicle

Classified before fixing (POLICY §2). Law: 0. Model: 0 (the class stays
0-for-4 all-time). Engine: 1, found before the corpus. Grader: 1, found by
the mutation pass.

| Defect | How it surfaced | What it forced |
|---|---|---|
| **The emitter listed a renamed entry document twice** — as its file-model event and as "scaffolded", because the pre-run Step-4 snapshot saw no `AGENTS.md` and the post snapshot did. | The manual end-to-end run on the upgrade fixture before any agent ran (a rename fixture). | Engine: a file-model event explains the entry document's presence, so it is excluded from the scaffolded derivation; unit `report_entry_document_listed_once_by_its_file_model_event`, red first. |
| **The grader's engine re-print crashed the whole mutation pass.** Under the `manifest_valid_json` mutation the manifest is unparsable; `engine report` exited 3 and the helper raised instead of recording a verdict — one corrupt manifest took 213 other kills with it. | The first mutation-pass invocation, on `upgrade`. | Grader: a failed re-print is a `failed` verdict carrying the engine's reason; the pass then ran 214/214. Same class as the audit re-run helper, which is now the one to watch. |

## Harness chronicle

No incidents. The workspace lock (BL-073) was live for the whole cycle: the
mutation pass ran after the runner exited, under its own lock, on the run's
own workspace.

## What this edition shipped

BL-075: the engine's `detect`, `apply`, `verify` and `report` jobs and the
run record (outside the repo, path derived from the root); SKILL.md Steps
1/3/6/7 as engine invocations; ADR-0006; the scaffold report as a graded
artifact; member #0 delivered through `engine apply` itself — whose
`verify` found this repository had never scaffolded `.gitattributes`
(v23's create-if-absent row) and named it. BL-076 (restructure emitter +
fidelity job) is the next edition's candidate.
