# Eval results — v7.4 (2026-07-10, constitution VERSION 7 unchanged)

Change under test: BL-004 restructure flow (diagnose → propose → approve →
apply; closed action set; grep-verifiable fidelity; kept paths immovable;
decision-gated conflicts) + BL-005b restructure eval scenario + BL-012 Wave 3
rider (Keep list heading pinned to `### Keep list`; candidates placement
fallback). New `references/restructure.md`. Procedure-only cycle — no
`assets/rules/**` change, VERSION stays 7.

| Scenario | Pass | v7.3 pass | Tokens (v7.3) | Wall time (v7.3) |
|---|---|---|---|---|
| fresh-scaffold-dotnet | 13/13 | 13/13 | 61,431 (67,884) | 122 s (127 s) |
| legacy-migration | 19/19 | 19/19 | 86,902 (90,055) | 299 s (255 s) |
| upgrade (add + retire + keep-add) | 13/13 | 13/13 | 66,420 (66,173) | 167 s (181 s) |
| audit (rotted, 10 defects + harvest; see note) | 17/17 (+1 heading marker) | 16/16 | 59,725 (63,353) | 148 s (167 s) |
| restructure (NEW) | 16/16 | — | 81,762 (—) | 307 s (—) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 52,071 (49,722) | 54 s (92 s) |
| idempotency:upgrade | 1/1 zero diff | 1/1 | 54,644 (54,468) | 81 s (121 s) |
| idempotency:restructure (NEW, permanent) | 1/1 zero diff | — | 72,788 (—) | 258 s (—) |

Static checks: all passing. Hook checks: 28/28.

Notes:
- **One mid-benchmark fixture defect caught and fixed, honestly recorded:**
  the first audit run refused to propose the planted harvest bait — and its
  reasoning was correct: "Always run **the archive importer** in dry-run
  mode…" names a project-specific component, so the generalizability test
  defensibly cuts both ways (v7.3's agent accepted it; this one didn't).
  Ambiguous bait is a fixture bug, not a skill bug. Fix: reworded to
  "Always run **data imports** in dry-run mode before a real import."
  (marker substring unchanged, commit 4d2dc05); the audit re-run
  (v7.4b workspace — its tokens/time above) passed 17/17 including the
  candidates appendix and the not-law suppression.
- **First live restructure run did everything the spec demands:** 7 items
  applied (move to `docs/superpowers/plans/`, `.cursorrules` merged +
  removed, ghost import fixed, dead index link retargeted, map corrected,
  kept file linked, owned drift + stale manifest healed via Steps 2–3 with
  the keep list carried), the planted CHANGELOG conflict surfaced as
  `[decision]` and left open under blanket approval, journal/overview
  findings routed to `For the team:` (no content invention), fidelity line
  reported, kept file byte-identical in place.
- **Restructure idempotency held:** the second run diagnosed a clean layer,
  proposed only the still-open decision item, applied nothing, and wrote
  nothing (`Fidelity: verified (0 lines tracked)`).
- Migration proposed four candidates this run (v7.3: two) — the extra two
  (repository-interfaces, thin-controllers) sit on the generalizability
  boundary since they name project paths. The grader's anchors (money rule
  proposed, branch convention excluded) are deliberately the unambiguous
  pair; boundary variance between runs is expected and ungraded.
- Token/time: every pre-existing scenario got cheaper or held; restructure
  is the second-priciest scenario (81.8k / 307 s) — justified, it runs an
  audit *plus* the repair. Idempotency runs stayed cheap.
