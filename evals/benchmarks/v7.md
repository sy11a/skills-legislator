# Eval results — VERSION 7 (2026-07-09)

Change under test: BL-006 CLAUDE.md.tpl v2 — ambient codebase map
(`docs/okf/codebase-map.md` + `@import`), `## Boundaries` section, glossary
pointer, hand-edit ban appended to `pair-development.md`.

| Scenario | Pass | v6 pass | Tokens (v6) | Wall time (v6) |
|---|---|---|---|---|
| fresh-scaffold-dotnet | 10/10 | 10/10 | 48,823 (43,602) | 183 s (119 s) |
| legacy-migration | 12/12 | not run at v6 | 52,080 (—) | 227 s (—) |
| upgrade (add + retire rule) | 10/10 | 10/10 | 45,659 (40,378) | 149 s (119 s) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 39,590 (38,781) | 313 s (102 s) |

Static checks: all passing (10 templates).

Notes:
- No pass-rate regression; all 33 assertions green. Token increase on fresh
  scaffold (+12%) and upgrade (+13%) matches the expected cost of two more
  artifacts and the extra map/boundaries derivation. Idempotency wall time
  ran long but the outcome (zero diff) is what the scenario asserts; timing
  variance across single runs is noise, not signal.
- The upgrade run's Step 7 correctly proposed the new CLAUDE.md wiring for an
  already-legislated repo: remove the retired rule's import, add
  pair-development.md's import, add the `@docs/okf/codebase-map.md` import, a
  glossary pointer, and a `## Boundaries` section — the Task 1 Step 7
  extension working as designed.
- Fresh scaffold verified beyond the grader: CLAUDE.md contains the map
  import, Boundaries section, and glossary pointer; the map table lists the
  actual top-level dirs with sensible descriptions.
- Observation (not a defect, candidate follow-up): in legacy-migration mode
  the CLAUDE.md rewrite includes only the 8 rule imports per
  `references/migration.md`; the map import / Boundaries / glossary pointer
  were left as Step 7 proposals. Since migration mode rewrites CLAUDE.md
  anyway, it could include the full v2 wiring directly — would need a
  migration.md update. Logged for backlog triage.
- pair-development.md doubled as the "newly added rule" in the upgrade
  fixture this round (it sorts last among core rules and was the rule
  actually edited at v7) — the propagation check exercised the real change.
