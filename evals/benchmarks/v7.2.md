# Eval results — v7.2 (2026-07-09, constitution VERSION 7 unchanged)

Change under test: BL-002 keep-markers (manifest `keep` list — carry-forward,
prompt-driven add, pinned serialization; audit check 10 enforced) + BL-011
riders (inline-code mentions count as references in checks 7/10, pinned
finding slugs, migration.md glossary quote aligned). Procedure-only cycle —
no `assets/rules/**` change, VERSION stays 7. Also on this base (disjoint
files, not exercised by these scenarios): BL-007 legislator-hooks plugin +
`evals/check_hooks.py` (26/26).

| Scenario | Pass | v7.1 pass | Tokens (v7.1) | Wall time (v7.1) |
|---|---|---|---|---|
| fresh-scaffold-dotnet | 13/13 (3 new keep checks) | 10/10 | 58,636 (49,062) | 261 s (238 s) |
| legacy-migration | 16/16 (3 new) | 13/13 | 74,009 (54,583) | 275 s (266 s) |
| upgrade (add + retire rule + keep-add) | 13/13 (3 new) | 10/10 | 73,761 (48,216) | 290 s (282 s) |
| audit (rotted fixture, now 10 defects) | 14/14 (+1 marker, +2 absent-markers) | 11/11 | 58,610 (45,819) | 270 s (184 s) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 49,722 (45,018) | 92 s (137 s) |
| idempotency:upgrade (NEW, permanent) | 1/1 zero diff | — | 54,468 (—) | 121 s (—) |

Static checks: all passing. Hook checks (`check_hooks.py`, new): 26/26.

Notes:
- No pass-rate regression anywhere; 58/58 assertions green including the
  nine new keep assertions (three per common-checks scenario), the tenth
  audit marker, and the two absent-markers.
- **The keep add-path worked first try:** the upgrade agent carried the
  fixture's `deploy-runbook` entry forward byte-identically and inserted the
  prompt-requested `perf-tuning` entry in sorted position with the exact
  quoted reason, in pinned single-line serialization.
- **BL-011's orphan fix verified live both ways:** the audit flagged the
  unlinked keep-listed `special-sauce.md` (check 10 enforcement) while NOT
  flagging `docs/okf/index.md`/`glossary.md` (inline-code mentions now count
  as references) — the exact false-positive pair the v7.1 final review
  caught.
- The audit report used the newly pinned finding slugs verbatim, which is
  what makes the absent-marker assertions greppable at all.
- **Populated-keep idempotency held:** the new permanent `idempotency:upgrade`
  scenario re-serialized the two-entry keep list with zero diff.
- Token costs up vs. v7.1 (+11–36% per scenario) — the larger SKILL.md
  (keep-list procedure + expanded audit checks) plus, in migration/upgrade,
  genuinely more work (keep handling, more Step 4 artifacts in the upgrade
  fixture path). Audit wall time up (270 s vs 184 s) with the tenth check
  and reference-definition scan. Worth watching at the next cycle; not a
  regression signal on its own since all behavior assertions strengthened.
- Idempotency runs got *faster* (92/121 s vs 137/313 s at v7/v7.1) — the
  second-run no-op path is short and the agents verified rather than
  rewrote.
