# Eval results — v7.1 (2026-07-09, constitution VERSION 7 unchanged)

Change under test: BL-001 audit mode (10 read-only checks, full report on
request + Health section in upgrade runs) + BL-005a rotted-layer fixture +
BL-010 (migration writes full CLAUDE.md v2 wiring directly; wording fixes).
Procedure-only cycle — no `assets/rules/**` change, VERSION stays 7.

| Scenario | Pass | v7 pass | Tokens (v7) | Wall time (v7) |
|---|---|---|---|---|
| fresh-scaffold-dotnet | 10/10 | 10/10 | 49,062 (48,823) | 238 s (183 s) |
| legacy-migration | 13/13 (new wiring check) | 12/12 | 54,583 (52,080) | 266 s (227 s) |
| upgrade (add + retire rule) | 10/10 | 10/10 | 48,216 (45,659) | 282 s (149 s) |
| audit (rotted fixture, NEW) | 11/11 | — | 45,819 (—) | 184 s (—) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 45,018 (39,590) | 137 s (313 s) |

Static checks: all passing.

Notes:
- No pass-rate regression anywhere; 45/45 assertions green including the two
  new checks (`claude_md_v2_wiring_written_directly` in migration, the whole
  audit scenario).
- **Audit's first live run found all nine planted defects** with correct
  severities and per-finding remedies, and verified its own zero-writes
  contract (status + HEAD identical before/after).
- **The Health section earned its keep immediately:** the upgrade run's
  Health flagged the dangling `@docs/ai/rules/core/retired-rule.md` import —
  a Critical created *by that very run's own deletion propagation* — and
  pointed at the removal already proposed in "Needs your review". This
  interplay (deletion propagation → dangling import → Health catches it) was
  not explicitly designed for; it fell out of the checks being honest.
- **Migration now writes the full v2 wiring directly** (map import,
  Boundaries, glossary pointer in the rewritten CLAUDE.md) instead of
  leaving Step 7 proposals — BL-010 behaving as intended, locked in by the
  new grader check.
- Token costs up modestly vs. v7 (+0.5–6% per scenario) — the price of the
  larger SKILL.md and, in upgrade mode, running Health. Upgrade wall time
  ran long (282 s vs 149 s) — single-run variance plus the Health checks;
  worth watching at v8 but not a signal yet.
- Idempotency zero-diff held with the Health section active (`Health:
  clean` on the second run — the section itself introduces no
  nondeterminism).
