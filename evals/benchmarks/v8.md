# Eval results — v8 (2026-07-10, constitution VERSION 7 → 8)

Change under test: BL-014 project rules — **first constitution-content
change**: new `core/project-rules.md` makes `.claude/rules/` the project-law
home (Claude Code native, auto-loaded, `paths:`-scopable). Full integration:
Step 4 scaffolds the directory; migration carves law-shaped legacy content
there; harvest scans it; audit check 11 (`project-rules`) flags conflicts
with owned law; restructure routes law-shaped merges there and
decision-gates conflicts. BL-013 riders shipped (fidelity-pass scoping +
fix-deletion exemption, restructure runs Step 0 first, restructure.md §5
definition reword, pinned decision-item shape).

| Scenario | Pass | v7.4 pass | Tokens (v7.4) | Wall time (v7.4) |
|---|---|---|---|---|
| fresh-scaffold-dotnet (re-run, see note 1) | 14/14 (+dir check) | 13/13 | 71,986 (61,431) | 142 s (122 s) |
| legacy-migration | 22/22 (+dir, +2 carve checks) | 19/19 | 118,536 (86,902) | 455 s (299 s) |
| upgrade (v7→v8: delivers project-rules.md itself) | 13/13 | 13/13 | 75,350 (66,420) | 169 s (167 s) |
| audit (11 defects, re-run, see note 2) | 18/18 (+1 marker) | 17/17 | 69,378 (59,725) | 202 s (148 s) |
| restructure (now TWO open decisions) | 17/17 (+1 decision-gate check) | 16/16 | 89,729 (81,762) | 364 s (307 s) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 54,949 (52,071) | 82 s (54 s) |
| idempotency:upgrade | 1/1 zero diff | 1/1 | 53,584 (54,644) | 92 s (81 s) |
| idempotency:restructure | 1/1 zero diff | 1/1 | 89,104 (72,788) | 323 s (258 s) |

Static checks: all passing. Hook checks: 28/28.

Notes — two mid-benchmark catches, both fixed and re-run honestly:

1. **Spec-vs-shipped gap caught by the upgrade run's own report:** the spec
   says the new rule is "imported via CLAUDE.md's block", but the template
   had only gained the pointer bullet — while migration mode (which imports
   one line per core file) WOULD import it, so fresh and migrated repos
   would diverge. Fix: `@docs/ai/rules/core/project-rules.md` added to the
   tpl's core block (commit d8c911a); fresh scenario re-run in the v8b
   workspace (its numbers above) — 14/14 with both the import line and the
   pointer bullet verified in the output.
2. **Suppression must be silent:** the first audit run correctly excluded
   the not-law-marked line from candidates but then narrated the exclusion
   in a Notes section, quoting the suppressed statement verbatim — tripping
   the absent-marker (17/18) and re-surfacing exactly what the user
   silenced. Fix: SKILL.md now says skipping is silent — never quote, list,
   or mention suppressed statements in any report (commit 4966342); audit
   re-run in the v8c workspace (its numbers above) — 18/18. Third
   literal-drift catch across five benchmarks; the pinned-format discipline
   keeps earning its keep.
3. **The v7→v8 upgrade path works end-to-end:** with `project-rules.md`
   sorting last, the fixture generator auto-withheld it, so the upgrade
   scenario tested delivering exactly this release — new rule copied in,
   retired rule deleted, manifest to v8, keep list carried, zero spurious
   diffs on the second run.
4. **Migration's three-way split behaved:** law carved to
   `.claude/rules/{architecture,pre-commit}.md`, instance data (branch
   convention, staging note) kept in CLAUDE.md, both graded directly.
   Restructure merged `.cursorrules` into `.claude/rules/testing.md` (new
   routing) and left BOTH conflicts (CLAUDE.md wiki-changelog note and the
   planted `journal.md` rule) as open decisions, byte-untouched — the
   second run re-surfaced them with zero writes.
5. Token costs: migration +36% (the carve adds real work: new files,
   three-way classification) — the priciest scenario yet at 118.5k; audit
   +16% (check 11); others within noise. Idempotency runs held cheap-ish;
   restructure's second run is the costliest no-op (89k) since it re-runs a
   full 11-check diagnose. Worth watching, not a regression: every
   assertion strengthened and pass rate is 87/87.
