# Eval results — v10 (2026-07-11, constitution VERSION 9 → 10)

Change under test: BL-018 branch discipline — `core/pair-development.md`
gains the integration law ("Integrating is part of the task": WIP limit 1,
stack dependent work bottom-up, batch micro-changes, cut from fresh main;
branch-per-task and user-only merges unchanged) and
`stacks/dotnet/architecture.md` gains the captive-dependency bullet
(BL-017 item 5). BL-017 riders 1–4 shipped: check 12 exempts kept paths,
recognizes `docs/okf/index.md` as a referrer, exempts conventional
community docs (CONTRIBUTING.md etc.), pins check-7/9/12 precedence, and
its finding text reads "not wired into the AI layer"; the upgrade fixture
now withholds the alphabetically last dotnet stack rule alongside the last
core rule, and the grader asserts both the delivered file and the report's
proposed `@…data-access.md` import line (the upgrade eval saves its Step 7
report like migration does).

| Scenario | Pass | v9 pass | Tokens (v9) | Wall time (v9) |
|---|---|---|---|---|
| fresh-scaffold-dotnet | 14/14 | 14/14 | 62,418 (64,875) | 161 s (137 s) |
| legacy-migration | 22/22 | 22/22 | 86,571 (91,495) | 317 s (399 s) |
| upgrade (v9→v10, +3 assertions) | 16/16 | 13/13 | 79,084 (71,230) | 292 s (214 s) |
| audit (12 defects) | 22/22 | 22/22 | 66,116 (72,099) | 186 s (209 s) |
| restructure | 21/21 | 21/21 | 85,085 (94,467) | 357 s (377 s) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 53,510 (49,920) | 71 s (57 s) |
| idempotency:upgrade | 1/1 zero diff | 1/1 | 59,516 (64,464) | 123 s (190 s) |
| idempotency:restructure | 1/1 zero diff | 1/1 | 81,368 (71,279) | 270 s (173 s) |

Total 98/98 (v9: 95/95; +3 assertions, all upgrade-scenario). Static
checks: all passing. Hook checks: 28/28.

Notes:

1. **Second consecutive single-pass clean benchmark** — no mid-run fixes,
   every scenario graded against shipped bytes.
2. **The v9 fleet-action gap is closed:** with both `project-rules.md`
   (core) and `data-access.md` (stack) withheld, the upgrade run delivered
   both files and its saved report proposed the exact
   `@docs/ai/rules/stacks/dotnet/data-access.md` import line — the path
   BL-017 item 4 flagged as never exercised.
3. **Rider 1 (keep exemption) fired unprompted in the wild:** the upgrade
   idempotency run's harvest noted it skipped `docs/notes/*.md` from the
   check-12 scan because both files are manifest keep entries — exactly
   the new exemption, applied by an agent that was never told about it
   beyond the SKILL.md text.
4. The pair-development amendment is procedure-neutral for the eval
   scenarios (none of them cut task branches), so its coverage is the
   static checks + fleet rollout; the eval suite measures it only
   indirectly. Behavioral coverage would need a scenario where the agent
   is asked to start a second task with an unmerged branch — logged as a
   possible future scenario, not built now (YAGNI until the rule
   misbehaves).
5. Token/time: audit −8%, restructure −10%, migration −5% vs v9; upgrade
   +11% with three more assertions and a saved report — expected. Nothing
   trending wrong; pass rate 98/98.
