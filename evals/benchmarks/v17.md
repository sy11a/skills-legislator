# Eval results — v17 (2026-08-21, constitution VERSION 16 → 17)

Change under test: **edition v17 — the SDD law.** New core rule
`core/sdd.md` (case home `docs/cases/BL-NNN/`, spec typology, EARS R-NNN,
clarify protocol, ceremony tiers, plan-as-package, analyze/converge
gates, cross-repo convention) + `cases-README.md.tpl` + the
artifact-lifecycle amendment (docs/cases/ home, **generated** role
class). Riders: **BL-028** (manifest key `profiles`→`stacks`, legacy
fallback), **BL-030** (constitution disambiguation sweep — "entry
document"), **BL-025** triage (items 1,2,4,5,6 shipped — severity
anchoring, link-skills hardening, uninstalled-skill clause, grader
scoping; item 3 excluded-stale: v14 derivation rework already dissolved
it). Plus, mid-benchmark, **seven law fixes the benchmark itself
forced** (the restructure saga, below).

| Scenario | Pass | v16 pass |
|---|---|---|
| fresh-scaffold-dotnet | 18/18 | 18/18 |
| legacy-migration | 26/26 | 26/26 |
| upgrade (stacks-key migration, keep add+carry) | 18/18 | 18/18 |
| audit (15 defects + severity anchoring) | 30/30 | 27/27 |
| restructure | 28/28 | 28/28 |
| idempotency ×3 | **pending — the last rung before merge** | 3/3 |

**Total 120/120 on the scenario corpus** (v16: 120/120; audit grew by
the three severity-anchored asserts, BL-025.2).

## Confounds — read the cost numbers as incomparable

1. **Model switch:** glm-5.3 → **glm-5-turbo** for all runs. glm-5.3
   dropped long streams on this endpoint all day (313 logged stream
   errors; a 480s probe produced 63 bytes); turbo streamed a 27 KB essay
   in one 5-minute probe. Tokens/wall are NOT comparable to v16 — the
   model, not the skill, dominates them.
2. **Historical prompts:** BL-036's trigger minimization ships later in
   the v17 cycle (Wave A); this corpus ran on the v16-era prompts.
3. **Provider turbulence shaped the harness, not the skill:** runs died
   to stream drops and were recovered by a resume ladder; the orchestrator
   (stall detection, fixture reset, auto-grade) is now de-facto
   infrastructure and lands as BL-037/`tools/evals-bg.sh`.

## The restructure saga — 17 runs, five law generations, every fix benchmark-caused

The suite worked exactly as designed: each red run was a diagnosis, and
most reds were **real defects** — in the skill's law or, twice, in the
grader itself. Generation stamps (`v17-<commit>`, in `grade-history.jsonl`)
keep the flaky analysis honest; cross-generation runs never vote.

| Gen | Fix forced by the runs | Nature |
|---|---|---|
| 0 | baseline law | — |
| 1 | dangling `@import` is a `[fix]`, never a `[decision]`; `.claude/rules/skills.md` edits are the owner's call | law |
| 2 | merge survivability: in-plan line inventory, deferred source deletion, idempotent inserts; verbatim report echo | law |
| 3 | deletion classes split: merge-source deletion (post-fidelity, part of merge) vs dead-content deletion (owner-only `[decision]`) | law |
| 4 | fix-scope closed: wiring/derived artifacts only — contradictions are decisions in any file; v14 rename preserves content verbatim; fidelity-exempt narrowed | law |
| 5 | revert scoped to the blocked item alone; one in-run re-apply carrying the lost line | law |
| — | grader: `## For the team:` heading regex (agent was right); fidelity `-i` + glossary markers (lawful table reformat); skill-binding routing equivalence (value over typography) | grader |

Final generation (`v17-47632e0`): 27/28 + 27/28, single identical
near-miss resolved by the routing-equivalence grader fix → 28/28.

## What the edition proves

- The **SDD law delivers**: fresh/upgrade/audit passed clean on first
  healthy runs — case home created, `stacks` key migrated losslessly,
  severity anchoring held, 15/15 defects named under correct headings.
- The **suite executes the law, not the prompt**: every restructure fix
  above was found by an assert, root-caused from the report, and
  verified by reruns — the benchmark's intended loop, working.

## Done when (remaining before merge)

- [ ] idempotency ×3 (fresh, upgrade, restructure) — zero-diff second
      runs per `evals/README.md`; needs run-1 commits in the fixtures.
- [ ] Backlog: BL-032 status update (corpus green; saga record here),
      BL-028/030/025-rides marked done riding; BL-036 Wave A next.
