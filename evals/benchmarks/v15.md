# Eval results — v15 (2026-08-16, constitution VERSION 14 → 15)

Change under test: **new core rule `artifact-lifecycle.md`** — the artifact
life contract, distilled from the knowledge-observability signal-over-noise
work (ADR-0034/0035 in that tool's repo, 2026-08-15):

- Seven laws: role declared at creation (reference vs lifecycle), completed
  lifecycle artifacts are history (never rewritten/staled/swept), reference
  artifacts die by review not silently, every worklist item is an action
  (noise classes excluded mechanically), alert thresholds derive from a
  single declared cadence, unknown classification fails toward the cheap
  error, no silent caps.
- Wiring: `@docs/ai/rules/core/artifact-lifecycle.md` import line added to
  `AGENTS.md.tpl` (core import block now 10 rules).
- No SKILL.md procedure changes; owned-file count grows by one
  (`ownedFiles` now 14 entries).

| Scenario | Pass | v14 pass |
|---|---|---|
| fresh-scaffold-dotnet | 18/18 | 18/18 |
| legacy-migration | 26/26 | 26/26 |
| upgrade (retired-rule deletion, keep-list add) | 18/18 | 18/18 |
| audit (15 defects) | 27/27 | 27/27 |
| restructure | 28/28 | 28/28 |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 |
| idempotency:upgrade | 1/1 zero diff | 1/1 |
| idempotency:restructure | 1/1 zero diff | 1/1 |

Total 120/120 (v14: 120/120). No new assertions: the graders derive
expectations from the skill source, so the new rule is absorbed by the
existing `owned_files_verbatim` / `manifest_ownedFiles_exact_sorted` /
`agents_md_imports_all_core` checks. With `artifact-lifecycle.md` added,
the alphabetically last core rule is still `verification.md`, so the
upgrade fixture's withheld-rule pair is unchanged from v14. Static checks:
all passing (the tpl-import check caught the missing import line during
development before any agent run — exactly its job).

Cost (per-scenario subagent tokens / wall time — instrumented this run,
which v14 skipped):

| Scenario | Tokens | Wall time |
|---|---:|---:|
| fresh-scaffold-dotnet | 67k | 2m29s |
| legacy-migration | 83k | 3m56s |
| upgrade | 71k | 3m10s |
| audit | 69k | 2m53s |
| restructure | 95k | 6m02s |
| idempotency ×3 | 60k + 63k + 63k | 1m38s + 1m40s + 2m25s |
| **Total** | **~571k** | (runs parallelized) |

Notes — full disclosure:

1. **Clean first-pass e2e — no re-runs.** All five scenarios and all three
   idempotency passes graded green on their first agent run. The new rule
   rode through every mode untouched: copied verbatim (fresh, migration),
   carried byte-identically (upgrade fixture already contained it),
   healed-over without drift (restructure), and re-copied to a zero diff
   (all idempotency passes).
2. **The upgrade agent's second run correctly re-surfaced** the pre-existing
   AGENTS.md dangling `retired-rule.md` import as a Health finding without
   touching the project-owned file — proposal-only, as designed.
3. **Idempotency:fresh detected mode "upgrade"** on the second run (manifest
   now exists at current VERSION) — correct per SKILL.md mode detection; the
   scenario name refers to the fixture, not the expected mode.
4. **Next: fleet propagation.** Downstream legislated repos pick up
   `artifact-lifecycle.md` via `/legislator` (upgrade mode); each repo's
   Step 7 report will propose the new `@import` line for its AGENTS.md.
