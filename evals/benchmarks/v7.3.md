# Eval results — v7.3 (2026-07-10, constitution VERSION 7 unchanged)

Change under test: BL-003 harvest (Constitution candidates — report-only
proposals in migration/upgrade reports and as an audit appendix; `<!--
legislator: not-law -->` suppression) + BL-012 riders (keep-refusal
reporting, Bash-copy mandate closing the write-guard seam). Procedure-only
cycle — no `assets/rules/**` change, VERSION stays 7.

| Scenario | Pass | v7.2 pass | Tokens (v7.2) | Wall time (v7.2) |
|---|---|---|---|---|
| fresh-scaffold-dotnet | 13/13 | 13/13 | 67,884 (58,636) | 127 s (261 s) |
| legacy-migration | 19/19 (3 new harvest checks; see note) | 16/16 | 90,055 (74,009) | 255 s (275 s) |
| upgrade (add + retire + keep-add) | 13/13 | 13/13 | 66,173 (73,761) | 181 s (290 s) |
| audit (rotted, 10 defects + harvest appendix) | 16/16 (+1 marker, +1 absent-marker) | 14/14 | 63,353 (58,610) | 167 s (270 s) |
| idempotency:fresh-scaffold-dotnet | 1/1 zero diff | 1/1 | 50,344 (49,722) | 59 s (92 s) |
| idempotency:upgrade | 1/1 zero diff | 1/1 | 55,617 (54,468) | 103 s (121 s) |

Static checks: all passing. Hook checks: 28/28 (v7.2's file recorded 26 — miscount inherited from the BL-007 report; the suite has been 28 checks since it landed).

Notes:
- **One mid-benchmark regression caught and fixed, honestly recorded:** the
  first migration run produced perfect harvest *content* (decimal-money +
  `dotnet format` proposed; branch convention and path-specific rules
  correctly excluded as instance data) but headed the section `##
  Constitution candidates` instead of the pinned `###` — failing both
  section-scoped checks (17/19). Fix: SKILL.md now pins the heading
  byte-for-byte (commit 7d3f36d, "wherever the report appears"). The re-run
  on a fresh fixture (v7.3b workspace, migration tokens/time above are the
  re-run's) passed 19/19. This is the eval suite working as designed:
  formatting drift a human reader would shrug at is exactly what breaks
  downstream grep-ability.
- **Harvest judgment was correct in all four emitting runs:** migration ×2
  proposed exactly the two generalizable laws and excluded instance data;
  the audit appendix quoted the planted candidate and suppressed the
  not-law-marked line (verified absent from the saved report); upgrade
  correctly found nothing law-shaped in its fixture and omitted the section.
- **BL-012 riders live:** the upgrade re-run correctly omitted the Keep list
  section (no changes, no refusals — the new trigger fires only on change or
  refusal); owned-file copies went through Bash `cp` per the new mandate
  (write-guard seam closed).
- Idempotency zero-diff held on both scenarios — harvest introduces no
  writes by construction, and the second upgrade run's manifest (populated
  keep list) was byte-identical.
- Token costs: migration +22% (harvest scan + the larger SKILL.md), audit
  +8%, fresh +16%; upgrade −10% and every wall time improved or held. The
  migration number is the priciest scenario yet (90k) — worth watching, but
  it carries the heaviest workload (full migration + carve-outs + harvest).
