---
case: BL-347
issue: 405
kind: Changed
date: 2026-09-19
---

## changelog

- The three rendered views get a composition law: one `## changelog` bullet per case pointing at its summary, `## okf-log` owed only where a concept's meaning changed and no case summary records it, and `## journal` carrying only dead ends, open questions and decisions — with `sdd-lint` gaining the one-bullet and journal-present checks, and `FragmentLint` its first tests.

## journal

- Dead end avoided rather than walked: the ruling that opened this work put the three surfaces' cut *against* the change-fragment design, on the premise that the fragment renderer had not been built. One `diff` between `skill/assets/rules/core/` and `docs/ai/rules/core/` showed it merged and undelivered, and the two turned out to be layers of one law — #327 rules where a telling is written, this rules how much it may say. The tracker said `backlog`; the tracker is a docket, not a record of what is built. Open for the next session: `FragmentLint` demands that **every** pending fragment match the current branch, but fragments accumulate until a release cut, so a second pending fragment fires on every branch but its own — it fires today on `L-3.md` from anywhere. The check wants inverting (the branch's case must have a fragment) rather than tightening, and that is not this case's work. Decided here and not escalated: the pre-existing `L-3.md` fragment was condensed to one bullet rather than exempted by date, because a fragment is a pending instruction to the renderer and not yet a record of the past.
