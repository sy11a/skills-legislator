---
case: BL-370
issue: https://github.com/sy11a/Architector/issues/435
kind: Fixed
date: 2026-09-20
---

## changelog

- The release matrix says something when it is red: the .NET gate reads a runner's coloured test summary instead of reporting a passing suite as "zero tests ran", proves its reader on seven controls before every run, names the tests of a red module, and the matrix names a macOS Intel runner that exists — three of the four RIDs are green for the first time, and the fourth is red on 71 real Windows failures ([BL-370](docs/cases/BL-370-ci-reads-its-runner/summary.md), `sy11a/Architector#435`).

## journal

- The reusable part is how long it stood: a check that never blocks a merge and is red from its first run is never read, so **two weeks of red hid that legislator does not pass its suite on Windows** — the finding was behind the noise the whole time. Dead end: reading the failures with `gh run view --log` and an ESC filter — `gh` rewrites ESC as the two characters `^[`, so three filters in a row matched nothing and reported an empty list that looked like "no failures". A one-run probe (`continue-on-error` on the Windows `Test`) bought the answer to "does anything behind the suite work" and was withdrawn in the next commit; the first attempt to withdraw it amended the probe's message instead of reverting it, and only the rejected push showed it. Put to the operator as port / drop the RID / build without the suite, and ruled the same day: **port** — the four-RID ruling stands, the Windows job stays red until BL-372 (`sy11a/Architector#436`) lands, and nothing is skipped to make it green. Not done here and worth a case: `check_static.py`, `check_engine.py` and `check_hooks.py` run in no workflow — BL-360 already paid for that once.
