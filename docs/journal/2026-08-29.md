# 2026-08-29

## BL-075 — edition v24 benchmarked and closed

The corpus went green in one pass — 214/214 — the first edition since v17
to need no law fix round; idempotency zero ×3 with the run record living
in `/tmp` exactly as designed. The mutation pass found the cycle's one
defect, in the grader: my engine re-print helper raised on a corrupt
manifest and took the whole pass down — a failed re-print is now a
`failed` verdict with the engine's reason, and the pass reads 214/214
killed. Member #0 was delivered by `engine apply` itself, and its `verify`
immediately earned its keep: this repository had never scaffolded
`.gitattributes` (a v23 create-if-absent row) — created from the template.
Measured, not felt: upgrade/migration/restructure runs are 20–35% shorter
and cheaper because Steps 3/6/7 are three invocations instead of a
dozen `cp`s and a hand-composed report; fresh scaffold unchanged; audit
slower this run, one data point, recorded as variance. Next in the queue:
BL-076 (restructure emitter + fidelity job, v25) — after the v24 tag and
fleet sweep.

## BL-077 — the outer-only pivot: brainstorm, spec, backlog re-cut

The owner opened the day with two ideas — the AI layer leaving the code
repository entirely, and the legislator as one installation per machine
configuring every project it will ever touch — and the session became the
design of edition v25. Decisions, all recorded in ADR-0007 and the spec's
clarifications: inner mode dies; the instance (a group of projects) is
the git unit; the code repo keeps zero tracked AI files; the machine
registry is the single truth about placement; kbl and fleet-obs become
modules, fleet-obs machine-wide with a *generated* registry plus a hand
overlay; config is code (which unblocked BL-067); default deterministic.
The link mechanism got a real critique: four candidates measured against
harness facts (Claude Code walks `CLAUDE.md` above `.git` but not rules;
opencode stops hard at the worktree; both attach an external config dir
by env — the M3 evolution that needs a CLI). Chosen: a two-file stub as
the channel, the global arms as sentinel, env-attach reserved. Dead end
noted: a clone inside the control tree — elegant, and opencode cannot
see it. Research went into the neighbours: kbl is pre-code with a fixed
canon; fleet-obs hardcodes the in-repo assumption in five places, hence
BL-080 must merge before v25. The backlog was revised: BL-027/044/045/
052/071 absorbed, BL-076 re-targeted to v26, BL-067 unblocked, new cases
BL-077…081, a new "Agreed order after v24". Side finding for the owner:
this machine's fleet-obs capture hooks point at the repository's
pre-rename path and have been silent since. Next: the plan package for
BL-077 (writing-plans), then the three spikes before the link code.

## BL-082 — the substrate pivot: .NET now, config everywhere, MCP later

The owner returned with three requirements on top of the morning's
pivot: all determinism on .NET (Python kept for trial runs only, built
to product standard through review sessions), everything about
environment and placement configurable with no literals, and an MCP
surface so the agent calls tested code instead of a script it can edit
mid-session. Four questions settled the shape (spec `## Clarifications`):
v25 is built on .NET from the start — BL-072 moves from "after v27" to
step zero, absorbing into BL-082; configurability stops at law (paths,
names, thresholds, floors are options; rules are text); MCP is a
fourth host of the same core, gated on coverage and parity, v27; the
code lives here under `src/`/`tests/`, .NET 10, xUnit v3 on MTP,
NativeAOT CLI. ADR-0008 amends ADR-0005's phasing and leaves its end
state intact. Dead end considered and kept only as a rule: Python
spikes inside cases (option C) — allowed for prototypes, not the plan.
Deferred on purpose: the codebase map and `index.md` tech stack line
wait for the v25 commit that creates `src/`, because `anchors` would
fail on a directory that does not exist yet. The eval harness stays
Python by classification (instrument, not arm); its future is a
separate question. Next: the plan package for BL-082 (writing-plans) —
the skeleton and the engine pilot port come before any BL-077 code.
