# 2026-08-29

## BL-075 — edition v24 benchmarked and closed

The corpus went green in one pass — 214/214 — the first edition since v17
to need no law fix round; idempotency zero ×3 with the run record living
in `/tmp` exactly as designed. The mutation pass found the cycle's one
defect, in the grader: my engine re-print helper raised on a corrupt
manifest and took the whole pass down — a failed re-print is now a
`failed` verdict with the engine's reason, and the pass reads 214/214
killed. Member #0 was delivered by `engine apply` itself, and its `verify`
immediately earned its keep: this repository had never scaffolded
`.gitattributes` (a v23 create-if-absent row) — created from the template.
Measured, not felt: upgrade/migration/restructure runs are 20–35% shorter
and cheaper because Steps 3/6/7 are three invocations instead of a
dozen `cp`s and a hand-composed report; fresh scaffold unchanged; audit
slower this run, one data point, recorded as variance. Next in the queue:
BL-076 (restructure emitter + fidelity job, v25) — after the v24 tag and
fleet sweep.
