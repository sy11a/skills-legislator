# 2026-08-31 — Monday

## Edition v25 — the tracker slots (BL-085, BL-087; cross-repo clerk BL-016)

- The companion product's migration line was blocked on us: its contract's
  invariant "law before migration" means no repository moves its work into a
  tracker while its own constitution still says work items become
  `docs/backlog.md` entries. So this edition cuts the slots and nothing more.
- Delivered: the `Task tracker:` pointer line in `AGENTS.md.tpl` (a pointer to
  the project's sources note, never a link to a tracker); `{{BACKLOG_BODY}}`
  in `backlog.md.tpl` with its derivation rule (queue body without a tracker,
  pointer body with one); the three law sentences naming both homes; audit
  check 18 `tracker-drift`; the companion named once in the README; Step 7.1's
  gated source-binding offer.
- The check is deliberately blind: it reads `docs/backlog.md` and the entry
  document, never a tracker. Two conditions — a work item above a generated
  mirror marker (the pointer region holds no items; below the marker is the
  companion's region, which no run of ours rewrites), and work items in a repo
  whose entry document records a tracker but whose file carries no mirror (two
  sources of truth). What counts as a work item is closed so the engine can
  execute it: a heading or bullet whose first token is a case key.
- Routing decided by the check-15/17 precedent: `tracker-drift` findings are
  never plan items. Moving an item into a tracker is a write outside the
  repository, which this skill never makes — they go to `## For the team:` and
  `docs/backlog.md` stays byte-unchanged.
- Numbering: v25 was on paper reserved for BL-082 + BL-077, but BL-082 is
  parked by the owner and BL-077 builds on it. Recorded the policy rather than
  the exception — edition numbers are assigned at merge, never reserved.
- Ruled against the cross-repo issue text on one point: BL-031 is **not**
  absorbed. The slots move the register into a tracker only where a repo
  records one; this repository records none, so its queue/register split
  remains a real case.
- Open: the e2e benchmark (~8 agent runs) has not been run — the static and
  engine suites are green, and POLICY.md's baseline-before-change rule can
  still be satisfied from `origin/master`, since the baseline is a property of
  the code and not of wall-clock order.

## BL-082 — stage 6, build: T-03 (the options model — the only home of defaults)

Red (per R-8209, R-8213): driver 1 — a `check_static.py` section "no path/name literal outside the options model" over every `.cs` under `src/` (hosts included — a host literal is a finding too, `.claude/rules/dotnet-substrate.md`), `ALLOWED` = `LegislatorOptions.cs` alone; the plan's regex kept verbatim after a dry probe: a clean PASS on the existing tree first (no false positive), then exactly one FAIL on a planted `"docs/okf"` in Engine's `Placeholder.cs` with the line named, rc=1, then PASS again once the plant was removed. Driver 2 — `LegislatorOptionsTests` (3 facts per the plan), red as CS0234 naming the missing `Legislator.Core.Options`. Red-review ruling: `Defaults_carry_the_Defaults_source` was vacuous on an empty `Enumerate()` (`Assert.All` passes on nothing) — strengthened at red-review with a count and at ownership with key-set equality (`KeyMap.Keys` == the keys `Enumerate()` yields), which also catches a mistyped or permuted key; proven by mutating `build_dirs` → 1 failed / 9 passed, then restored. Coupling note: the unit drivers pin C-03's shape — `OptionValue<T>(Value, Source)`, a hand-written static `KeyMap`, a tuple-yielding `Enumerate()`, lists comma-joined; the static check is a tripwire over the v24 constant surface, not a proof — `".gitignore"` slips past `\.git\b`, a bare `"git"` is not listed (its home is `GitExecutable`), a key such as `"docs_dir"` outside ALLOWED would be caught only by the `docs` prefix.

Green: `Options/OptionsLayer.cs` (the layer enum and the `OptionValue<T>` record together — two small provenance types in one file; the plan named two files for C-03's three types) and `Options/LegislatorOptions.cs` — 26 members exactly per C-03, each default there and nowhere else; the eleven keys C-03 leaves unnamed are the snake_case of the member name (`opencode_config`, `git_timeout_seconds`, …); `KeyMap` and `Enumerate()` hand-written (numbers `InvariantCulture`, lists `string.Join(",")`) — no reflection in Core, AOT-clean; the only reflection is the test-side member census. Colours matched prediction throughout, no R-006 stop. Final: static all ok, `check_dotnet.sh` rc=0 — build 0/0, tests 10/10, NativeAOT publish exit 0; engine, hooks, opencode (29/29), anchors and sdd-lint gates green.

Two owner questions at ownership, answered and recorded. (1) Case-sensitivity of the file-name defaults: the values are integration contracts (Claude Code reads `CLAUDE.md`, opencode `AGENTS.md`, Keep a Changelog `CHANGELOG.md`), so their case is not ours to vary; the exposure is case-insensitive checkouts (Windows/macOS fleet members, ADR-0005) where an existence probe matches a differently-cased file and a delivered `CHANGELOG.md` beside an existing `Changelog.md` becomes a git case-collision at merge or checkout, plus our own path comparisons, which must be ordinal on every OS with `MockFileSystem`'s case policy pinned in tests — a follow-up for T-06/T-07 (handoff open question; backlog candidate). (2) Defaults in a config file behind DI: overrides arrive in T-04 (Machine, Instance, Environment layers — the `OptionsLayer` enum is their seam); the defaults stay in code by R-8209 and ADR-0008 (one typed home, zero-config bootstrap, no shipped file to drift), and the host composes `LegislatorOptions` and injects it by constructor — DI by shape, no container (AOT).

## BL-082 — stage 6, build: T-04 (configuration layers, loud validation, provenance)

Red (per R-8210, R-8211, R-8212): the plan's eight facts plus guards from C-04 and the blast radius — a 26-key settability census (`Every_key_is_settable_from_a_layer`, a Theory over `KeyMap.Keys`: the composer's key → member application is hand-written, no reflection in Core, so a missed case would silently leave Defaults), nested-YAML rejection naming the key, all-layers error collection first layer first, env list comma-split, and six validator facts (unknown key with layer, int below one, empty string, `..` segment across `/`, `\` and `,`, `a..b` allowed). Red seen as 22 compile errors, every one CS0103/CS0246 naming one of the four missing types; a 23rd (CS0117 `IntegerKeys`) once the red-review's design added the integer-key census. Coupling note accepted at red-review: `LegislatorOptions` becomes a `sealed record` (the plan's `with`-expressions need it), `YamlLayerReader.Read` takes the layer so structural faults become `OptionsError`s with the right layer, lists are comma-split in one place.

Green: `Options/OptionsError.cs` (error record + `OptionsException`, message joined with `'\n'` — `Environment.NewLine` is a forbidden static), `Options/YamlLayerReader.cs` (YamlDotNet event stream, scalars and sequences of scalars, everything else an error, `SkipThisAndNestedEvents`), `Options/EnvLayerReader.cs` (`LEGISLATOR_` + upper-cased key, known keys only), `Options/OptionsValidator.cs` (`Judge`: unknown → integer ≥ 1 via `IntegerKeys` → non-empty → no `..` segment), `Options/OptionsComposer.cs` (read Machine, Instance, Env; validate each; throw all errors before applying; `Apply` switches on the member name via `nameof` so no key literal lives outside `LegislatorOptions.cs` — `"docs_dir"` would trip the R-8209 tripwire by its `docs` prefix). `LegislatorOptions.cs` gains `ListSeparator` and `IntegerKeys`; `Enumerate()` joins on the constant. One process event ⚠ at green: the first build stopped on CA1859 (`List` must return `string[]`), the "build 0/0" prediction missed by one line; every test colour matched — 56/56 (10 + 12 + 1 + 8 facts + 25 Theory cases, predicted 56), and the census guard proven biting by dropping the `BuildDirs` case: exactly 3 failed (`build_dirs` census + the two list facts), restored. Final: static ok, `check_dotnet.sh` build 0/0, tests 56/56, NativeAOT publish exit 0 (930 KB — the composer is not yet reached from the CLI, so trimmed away until T-05); engine, hooks, opencode (29/29), anchors, sdd-lint green.

Ownership 2026-08-31: diff owned as built, no correction. Recorded: duplicate YAML keys — last wins (R-8211 names no such fault); env unknown keys invisible (`IEnvironment` has no enumeration) — open question for T-05; document-level faults carry an empty `Key` — T-05 decides the rendering; `YamlStaticContext.cs` not needed. Contracts C-03/C-04 amended in place with dated notes.
