# 2026-09-06 — Sunday

## L-2 — fleet law: task entry defaults to `/autoflow` (Architector Release 0, track E)

- Driven unattended under `/autoflow`, as one task of Architector's Release 0
  kernel launch (its `docs/cases/BL-008-release-cycle/`, epic E-5, story
  S-5.1). The operator's rulings for this run were given in advance in the
  launch line rather than opened as a live window; recorded verbatim into
  `docs/cases/L-2-autoflow-entry-line/spec.md`'s `## Decision policy` and
  this task's tracker-equivalent (the case file itself — this repository has
  no tracker).
- Paid the second half of dev-flow BL-051's ruling 11: one line in
  `core/pair-development.md` stating that a task is opened via `/autoflow`
  unless the operator names `/flow`, conditioned on the skill pair being
  installed (silent, not a violation, where it is not). The skill half
  shipped in dev-flow's own repository and has been running daily since
  2026-09-05.
- Also updated this repository's own `.claude/rules/skills.md` directly —
  a `flow-sessions` class naming `autoflow` first. This is instance data
  (per `core/project-rules.md`, created once, edited per repository) and
  not a change to the delivered law stratum, so it needed no VERSION bump
  of its own.
- Decision recorded as ADR-0009: name the skill's command names directly in
  the law line (consistent with how `.claude/rules/skills.md` already names
  concrete installed skills), rather than describing the convention
  abstractly.
- **Left open, deliberately:** `skill/VERSION` is not bumped in this task.
  `constitution-source.md` ties a `skill/assets/rules/**` edit to a version
  bump, but this run's decision policy (DP-1) explicitly withholds that
  choice — an edition's number is the operator's plan, assigned at merge,
  never reserved (the precedent `docs/backlog.md` already records for
  edition v25). The bump is proposed in the pull request body, not decided
  here. Delivering the line into `docs/ai/rules/**` — here and across the
  fleet — waits on that ruling and the ordinary release runbook.
- Gates run before pushing: `python3 evals/check_static.py`,
  `python3 evals/check_engine.py`, `python3 evals/check_hooks.py`,
  `node evals/check_opencode_plugin.mjs` — no e2e benchmark, since nothing
  under `skill/SKILL.md`, `assets/templates/**`, or `references/**` changed
  and no behavior of a legislator *run* changed; only a rule file's prose
  and this repository's own instance data did.
- Open question for the operator, carried in the PR body rather than
  resolved here: which edition number this line ships under, and whether it
  rides with v26 or opens its own.
