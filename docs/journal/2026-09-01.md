# 2026-09-01

## BL-082 — stage 6, build: T-07 (the pilot port — `anchors`, the two-root classifier, the first twins)

Orientation found the plan's Step 9 selector broken before a line was written: `python3 evals/parity_labels.py | grep -E '^engine\t(anchor|symbol|path|human|scannable)'` returns nothing, because the rulers' labels are prose sentences and the `anchors_clean_repo_exit_0` shape the plan's example assumes exists nowhere in `check_engine.py`. A selector that silently returns zero reads as "no twins needed", which is the one thing the ledger exists to prevent — the same class of defect T-06 found in the plan's label regex. Ownership was re-derived by ruler section instead: eight labels in the `== anchors: … ==` blocks, plus `a repo with no docs/okf/ is clean, not an error`, the two `removed_doc_not_anchored` labels, the two `nested_build_output_ignored` / `real_source_still_resolves` labels, the two `crash_exits_distinctly` labels and two of the three `exit_codes_unchanged` labels — seventeen. The third (`usage error is 2`) and the two `== usage ==` labels are the host's dispatch, green since T-05, and stay with the CLI task rather than being counted as anchors coverage. The ledger confirmed the arithmetic independently before it was written down: with the twins in place it reported "the ledger fell to 179", and 196 − 17 = 179.

Red (per R-8205, R-8206, R-8204, R-8209): `AnchorClassifierTests` (17 Theory rows over the closed definition in `core/okf.md`, plus `PathTarget` and `MemberStem`), `SymbolIndexTests` (6), `AnchorsJobTests` (14 — one per branch of the Python job), `RepoLayoutTests` (3), `FindingsTests` (3), `JobRegistryTests` (3, the vacuous-until-now set T-05 deferred to the first registration), `AnchorsTwins` (17), and the ledger's orphan direction, also deferred from T-06 as vacuous over zero twins. Predicted: build fails, errors only under `tests/`, each naming a type T-07 must create, no warnings. Seen: 24 errors, all CS0234/CS0246/CS0103 under `tests/`, nothing in `src/` — with one miss, two CA1859 findings in the helper return types, the third instance of "an analyzer fires on code the agent wrote" after T-04 and T-05, applied at once rather than carried to profile sync. Because `Legislator.Parity.Tests` depends only on `Cli`, it compiles and the twins actually run: 20 tests, 18 red, 2 green — a behavioural red rather than only a compile red, which is what R-8206 asks for. The two green were the ledger's own siblings. The record is `docs/cases/BL-082-dotnet-deterministic-substrate/anchors-red.txt`.

Two twins were green against the empty registry on the first pass and were strengthened rather than cut: `removed_doc_not_anchored` asserts only that a document is absent from the output, and `crash_exits_distinctly (control)` only that stdout is empty — both of which a job that never ran satisfies. H-007 says cut a vacuous test; R-8206 says every label needs a twin; the resolution is to assert more than the ruler does, so the first now pins the whole of stdout (the removed document is silent because the live sibling spoke) and the second pins exit 3 alongside the silence. These are two of the roughly thirty-five labels the T-08 note predicted could not be shown red against an empty implementation — the estimate is holding.

Red-review ruling: the set accepted with eleven departures from the plan's letter — the broken selector; the three host labels left with the CLI task; C-07's `PathTarget` split into two pure functions because the Python probes the disk in its `Type.Member()` branch and a pure signature cannot; the plan's `"src/A.cs.Run()" → "src/A.cs"` expectation, which holds only if `src/A.cs.cs` exists; the PASCAL pattern taken from the engine (`(\.[A-Z][A-Za-z0-9]+)*`) rather than the plan's (`(\.[A-Za-z0-9_]+)*`), which would admit `WidgetStore.flush` and `Widget_Store`; `SourceRoots` added to `SymbolIndex` because the finding text names the roots searched; `MaxFileBytes` added to the options model, the Python's `MAX_BYTES` being a threshold; the crash fixture's mechanism; the two strengthened twins; the orphan ledger test; and the CA1859 repeat fixed inside the red step. A twelfth surfaced during green: C-07 places `Findings` in `Legislator.Core`, but its return type `JobResult` lives in `Legislator.Engine` and Core cannot reference Engine — `Findings` moved to `Legislator.Engine.Text`, leaving C-05 untouched.

Green: `Core/Repo/RepoLayout.cs` (every directory from the options model, segments joined with `/` because these paths are compared against document text and printed into findings), `Engine/Text/Findings.cs` (ordinal sort, one finding per line, exit 1 when there is one), `Engine/Anchors/AnchorKind.cs`, `AnchorClassifier.cs` (the closed definition, `[GeneratedRegex]` per the stage-4 amendment — a `new Regex(...)` falls back to the interpreter under NativeAOT), `SymbolIndex.cs` (source roots, hidden directories and build output at any depth excluded, the size ceiling from options) and `Jobs/AnchorsJob.cs`, registered in `JobRegistry`. The bundle walk uses `EnumerateFileSystemEntries`, not `EnumerateFiles`, because the Python's `rglob` yields directories too and the difference is silent.

The green prediction stated four claims and two held. Build was 0 errors and 0 warnings as claimed. Publish was exit 0 with no trim warnings, 3 445 624 bytes against T-06's 3 349 944. The test claim (145) was wrong by one — 146 — and the first run was 5 red, of which two were real finds. The options census caught `max_file_bytes` present in `KeyMap` but missing from `OptionsComposer.Apply`'s hand-written switch, which is precisely the silent fall-through T-04 built it to catch. And `MockFileSystem.File.ReadAllText` returns empty text for a directory where `System.IO` throws `UnauthorizedAccessException`: the expected red arrived green because the fake lies, not because the code was wrong. Rather than tweak the assertion to the fake's behaviour, the fixtures climbed a rung — `TempDirectory` in `Legislator.TestSupport`, a real directory on a real disk, with a `*.md` entry that is a directory as the portable stand-in for the ruler's `chmod 0o000` (which says nothing on Windows, and is why the ruler skips the case there under R-704).

The fourth claim — the ruler moving from 19 ok to 36 — failed, and the failure was the task's main finding. The ruler on the binary read 19 ok / 123 FAIL, unmoved, with every anchors check reporting `exit=2 out=''`, while the same binary answered correctly by hand. The cause: the environment layer admits every `LEGISLATOR_*` name as the option key it spells and refuses an unknown one by name (R-8210, the T-05 ruling that rejected ignoring what it does not recognise as a written-down defect) — so `LEGISLATOR_ENGINE_CMD`, the variable T-06 gave the rulers, made the arm exit 2 before it reached a job. The ruler was measuring the collision, not the port. T-06 could not have seen it: the registry was empty, every job exited 2 anyway, and the fault hid inside the expected failure — the identical hazard that made the ledger a ratchet instead of a test left red for five tasks. Measured behind a wrapper that strips the variable, parity was already correct: 34 ok / 108 FAIL, all seventeen owned labels `ok`.

Decision-gate stop, resolved by the operator (option a of four): the rulers move out of the namespace, to `PARITY_ENGINE_CMD` and `PARITY_HOOK_CMD`. Option (c), reserving a sub-prefix inside `LEGISLATOR_*`, was rejected as (a) plus a product change — the rename happens either way, and (c) only makes the variable look like it belongs to the binary while putting a hole in the model for a test tool's benefit. Option (b), ignoring unknown names, would have overturned the T-05 ruling to fix a harness fault. Option (d), passing the arm as an argument rather than an environment variable — which kills the class, since an argument is never inherited by a child process — is the better shape on principle but rewrites an interface the plan designs three tasks ahead at Task 12, so it belongs to a plan amendment on its own merits, not to a side effect of T-07. The separable question, whether an unrecognised `LEGISLATOR_*` variable should be fatal at all, is filed as BL-089. C-06 and the plan carry dated amendments; the journal entries of 2026-08-29 and 2026-08-31 name the old variables and were left alone, being records of what was true when written.

Final: the seven-check gate green — `check_static`, `check_engine`, `check_hooks`, `check_opencode_plugin` (29/29), `engine anchors`, `engine sdd-lint`, and `check_dotnet.sh` rc=0 with build 0/0, tests 146/146 and NativeAOT publish exit 0. On the binary the engine ruler is 34 ok / 108 FAIL with every owned label `ok`; both rulers unset are unmoved. `LedgerDebt` is 179.

## BL-082 — stage 6, build: T-08 first half (the git seam and `okf-debt`)

Ownership of the ruler was re-derived by section again: the plan's Step-5
selector returns nothing useful, and the three jobs of Task 8 own 57 labels
between them — `okf-debt` 11, `sdd-lint` 38, `baseline` 8. This session took the
first of the three; the task stays open.

The red was two observations, because the twins live in a project that compiles
without the unit sets: 68 compile errors naming `GitLog` and `OkfDebtJob`, and
all eleven `okf-debt` twins failing on the host's exit 2 for an unknown job. The
two carried case-sensitivity tests were measured first and came up green as
predicted — they pin the code's ordinal comparison (`Glossary.md` is anchored,
`Src/` is not `src`) rather than the fake's own case policy, so they say the
same thing on every operating system. The recorded red is
`docs/cases/BL-082-dotnet-deterministic-substrate/okf-debt-red.txt`.

Two labels of the eleven assert an absence and would have passed vacuously
against an empty registry; both were strengthened rather than cut, as T-07 did:
the stdout control now asserts exit 3 and a stderr naming git beside the
silence, and `removed_doc_no_debt` pins the whole of stdout instead of the
absence of one name. Measuring the arm afterwards showed why that matters —
four of the eleven labels were already green on the *ruler* before the port
existed, three because the ruler runs those two checks through a PATH shim on
`python3` directly rather than through the arm variable, and one because it
asserts an absence. The ruler is therefore not fully arm-parameterised; the
twins are what actually covers those four.

Green: 181/181 (146 + 11 twins + 22 units + 2 characterization), build 0/0,
static rulers unchanged, AOT publish exit 0. On the binary the engine ruler
reads **41 ok / 101 FAIL**, every `okf-debt` label ok, and the ledger fell
179 → 168. The published artifact grew 3 445 624 → 4 146 184 bytes, a fifth
larger for one job — date parsing is the suspect and it is a T-12 question
(the startup budget), noted here rather than chased now.

## BL-082 — stage 6, build: T-08 second half (`sdd-lint`)

Thirty-eight twins, the largest label block of the case, all shown red before a
line of the lint existed. The red taught something about the instrument rather
than the port: `csc` binds method bodies only after a clean declaration phase,
so two missing-type errors in one test file hid every other compile error in
the project. The full picture needed that file held aside for one build — worth
knowing, because a red measured only by "how many errors" would have read as
two when it was four.

The lint is seven passes over two scopes: coverage, dangling references and
placeholders per case; then the ADR, journal, changelog and OKF-front-matter
shapes over the tree. `CaseModel` and `Prose` carry what both the lint and the
baseline need — the three definition forms, fences blanked with inline code
kept, and the line-anchored converge marker that makes a quoted verdict a
quotation. `RepoLayout` gained `Adr`, `Journal` and `Changelog`, so every
directory the lint reads is the options model's, and a unit test renames two of
them to prove it. The contract's `CaseFile` shape was wrong for the same reason
C-07's `Findings` placement was: it listed requirement **ids** where the
baseline needs their definition text; amended in place.

Green on the first pass: 243/243, build 0 warnings after two analyzer findings
fixed inside the step — CA1859 for the fourth time in this case, and CA1875 new.
On the binary the engine ruler reads **78 ok / 64 FAIL**; every `lint_*` and
`sdd_*` label is ok and the only labels left in the two ported sections are the
eight `baseline_*` ones. The ledger fell 168 → 130. The artifact grew
4 146 184 → 4 308 104 bytes.

