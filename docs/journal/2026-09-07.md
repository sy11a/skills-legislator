# 2026-09-07 — Monday

## L-2 — the generated stage map pins `flow-sessions` (session 3 of stage 6)

- Continued the stage-6 build slice on `bl/l2-autoflow-entry-line` (mode
  autoflow, tier 0, driven under the case's decision policy): the
  generated-stage-map half of dev-flow BL-051's ruling 11. Session 1
  (commit `f368256`) delivered the law line and this repository's instance
  class; this session pins the generated side.
- The `{{SANCTIONED_SKILLS_BY_STAGE}}` derivation bullet in `skill/SKILL.md`
  now carries `flow-sessions` — `autoflow` (task entry; unattended default
  per `core/pair-development.md`) — as the first pinned stage affinity.
  The derivation is include-only-when-installed by its own pinned rule, and
  `skills-rules.md.tpl` needed no change: it interpolates the derived list
  verbatim, so the bullet's text is the whole content of a fresh
  repository's generated stage map.
- Red first, per the flow's red discipline: a structural probe on the
  derivation bullet showed the flow-sessions affinity absent before the
  edit (the failure named the defect — no legislator run could emit the
  class), with the shipped instance class as the green sibling and the four
  static gates as the working harness. Verifiability rung: scripted
  structural probe, chosen because the e2e benchmark (the boundary
  instrument for a behavioral `skill/` change) is the orchestrator-side
  run that stage 8 judges.
- ADR-0009's decision 2 gained the generated-side sentence — as committed,
  it covered only the instance half of ruling 11, while ruling 11 named
  "the generated stage map" too. Amendment is lawful on this branch: the
  ADR is undelivered (no edition carries it yet; the edition question stays
  the operator's, per DP-1).
- Doc homes updated per the repository's own law: glossary `flow-sessions`
  row (second home, `skill/SKILL.md`; timestamp bumped), changelog
  Unreleased `L-2` clause, backlog `L-2` row's What paragraph, this
  journal, and the OKF log entry.
- Gates: `python3 evals/check_static.py`, `python3 evals/check_engine.py`,
  `python3 evals/check_hooks.py`, `node evals/check_opencode_plugin.mjs` —
  green. The full e2e benchmark is deliberately NOT run in this session:
  it is the orchestrator's post-stage run and stage 8's evidence (the
  runner profile is frozen per `evals/benchmarks/v25.md`; the full staged
  corpus runs — no scoping ruling exists for L-2).
- Open question carried for the operator, unchanged: which edition number
  this line ships under — v26 with the next batch, or its own.

## L-2 — acceptance: converge clean (stage 8)

- Stage 8 converged the delivered work against the case spec's promises,
  ADR-0009 (including the decision-2 amendment), ruling 11's two halves,
  and the constitutional MUSTs. Every scenario of the frozen cycle-1 set
  passed on artifacts read at the judged version, not on handoff claims;
  the full gate set was re-run inside the stage (all four checks, plus
  `docs/ai/engine.py anchors` and `sdd-lint` — green and silent). The
  protocol and converge record live at
  `docs/cases/L-2-autoflow-entry-line/acceptance.md`.
- Two converge findings, both fixed in-stage. F-1 (missing): the benchmark
  record existed only untracked and held three absolute `/tmp` workspace
  paths — masked to `<eval-ws-N>` placeholders per `.claude/rules/records.md`
  and committed, so the branch now carries its own evidence. F-2
  (contradicts): the backlog `L-2` row claimed a draft PR was pushed —
  `gh pr list` shows none; the status line was corrected (branch carried,
  PR opens at delivery). Neither is CRITICAL: record and register accuracy,
  no constitutional breach.
- One deliberate non-finding, recorded so it does not resurface: the branch
  edits `skill/assets/rules/**` without the `constitution-source.md` version
  bump. That deferral is the operator's own recorded ruling (DP-1; ADR-0009
  decision 3) and is honored at the merge boundary where the edition is
  assigned — the PR body must carry the proposal.
- A-5.1.1's structural half stays carried: it reads the delivered law in a
  governed, re-legislated repository — the release runbook's read, after
  the operator rules on the edition. Stand fate: nothing deployed; the
  branch is the deliverable, pushed at stage 9 as the run's one sanctioned
  push.

## L-2 — delivery: draft PR #41 opened (stage 9)

- Draft PR #41 opened against `master` (body per the six-section MR
  template plus the run's decision ledger; the edition proposal v26 rides
  as an open question; no attribution anywhere; not marked ready, not
  merged). The run's one sanctioned push carried the three unpushed
  commits (`89ce886`, `963c935`, `34f0446`) plus this session's
  register/journal commit.
- Order note: the PR was opened before the register commit, so the `L-2`
  row names a PR that already existed when the row was written (the F-2
  lesson) and the whole delivery still fit the single sanctioned push.
- `master` moved during the task (PR #40 merged the BL-088/BL-089 register
  rows) and was left unintegrated on purpose: the branch is the tree stage
  8 judged, and a merge would change it post-converge; the BL-089 row
  duplication is the owner's reconciliation (DP-1 sanctions only the L-2
  row on this run's authority). `git merge-tree` shows no conflicts, so
  the PR merges cleanly as is.
- Reviewer round: empty — `gh` pulls no comments and no reviews on the
  fresh draft (solo project, no bots), so the review-rework loop closes
  with zero rows in the review ledger.
- Stand/stash fate: nothing deployed (no live stands exist), stash unused.
  Didn't-go-in delta: the edition question (PR body) and A-5.1.1's
  structural half (release runbook, post-edition) — both homed; no new
  backlog items filed (DP-1 confines this run's register writes to the
  L-2 row).

## BL-082 — the branch surfaces, and track E is ordered behind it

- A census of the repository, asked for at session open: the
  `bl/082-dotnet-deterministic-substrate` branch had not moved since
  2026-09-04 and existed **only on this machine** — no upstream, no pull
  request, nineteen commits and an uncommitted T-13 in one copy. Pushed as
  it stood.
- T-13's working tree is deliberately red and could not ride the branch:
  the repository's gate law wants `check_static.py` and `check_engine.py`
  green on every commit, and T-13 mid-flight is 4 static failures (checks
  15/17 already name the binary while the assertions still require the
  python3-absent wording), 1 in `check_engine.py` and 2 in
  `check_hooks.py` against `artifacts/linux-x64/legislator`. Snapshotted
  off-branch instead, on the owner's word: `wip/bl-082-t13-snapshot`,
  built through a temporary index so neither the branch nor its working
  tree moved. The commit message states the red and why it is not on the
  task branch.
- **The register was lying about the case.** `docs/backlog.md` still read
  "BL-082 parked 2026-08-29 … agents do not start it", while the case has
  been in progress under dev-flow since 2026-08-30 — the park's own
  condition (the owner takes it up himself) having been met. That is not a
  cosmetic error: by Release-0 amendment A-12 the kernel launches track E
  here on its own authority, and a register that reports the repository
  free is what lets it. Corrected in two places, with the branch, the
  task position and the unassigned edition number named.
- **Ordering ruled by the owner: BL-088 (`L-1`) waits for BL-082's merge.**
  Its third deliverable is an audit check, and T-13 replaces the arm every
  check executes in; run before the substrate lands, the check is written
  against an engine that is being deleted and written twice. Recorded on
  the item, on its stop condition and in the agreed order's item 8. The
  ruling still has to reach the kernel's own ledger — that is the
  operator's act, not this repository's.

## BL-082 — the branch takes master, and two keys move

- Rebased `bl/082-dotnet-deterministic-substrate` onto `master` (21 commits,
  the v25 merge flattened out) so T-14's benchmark measures the edition that
  will actually merge: master carried L-2's law line in
  `core/pair-development.md` and the `flow-sessions` affinity in `SKILL.md`,
  both of which the corpus reads. A backup tag `bl082-prerebase-20260907`
  holds the pre-rebase tip.
- Every conflict was a record file — journals, `CHANGELOG.md`,
  `docs/backlog.md`, the OKF glossary and log — resolved as unions, with the
  substrate and case trees taking the replayed commit's text since master
  never touched them. Two case records (`hooks-red.txt`,
  `parity-hooks-green.txt`) came back **unmasked** from a revision predating
  their masking; `check_static.py` caught the absolute path and they were
  re-masked in their own commit. Worth keeping: an automated conflict rule
  can silently undo a compliance fix made later in the same branch, and the
  static gate is what makes that visible.
- **The key collision resolved: the branch's `BL-088` and `BL-089` became
  `BL-091` and `BL-092`.** `master` had minted those two keys for the
  Release-0 pair (waterflow, the `/autoflow` entry line), and those are
  referenced from another repository's release ledger — the published key
  stays put and the unpublished rows move. The journal entries of 2026-08-31
  and 2026-09-01 and the OKF log name the old keys and were left as written,
  the way this repo already treats records of what was true when written; the
  moved rows carry a key note saying so.
- Gates after the rebase: 656 .NET tests, `check_engine`, `check_hooks`,
  `check_static`, `check_opencode_plugin`, `anchors`, `sdd-lint` — all green.

## BL-082 — T-14, the documentation half

- Wrote the bundle before the benchmark, not after it, so the corpus runs
  against documentation that already says what the edition is: the codebase
  map's package and plugin rows, `index.md`'s stack sentence (C# first,
  Python named as an instrument that ships in no edition), and the two terms
  the edition coined — `arm integrity` and `startup budget`.
- `.claude/rules/dotnet-substrate.md` now names the enforcing check at the end
  of every bullet, and writes "a person" where that is the truth: the
  logic-versus-wiring rule and the red-first discipline have no mechanical
  check. Naming the two honestly is worth more than the appearance of full
  coverage. Rewritten twice — the first version ran to 49 lines against
  `project-rules.md`'s ~30-line aim for an unscoped rule file, and was cut to
  35 by dropping prose rather than enforcement names.
- `evals/benchmarks/v26.md` exists as a **declared-incomplete** record: harness
  and checkpoint filled (656 .NET tests, both rulers green on the binary,
  3.3 ms startup median, the one released digest), every corpus row `pending`,
  and the four confounds a v24 comparison must hold — the retired scenario and
  the smaller total, the renamed instrument, the audit's moved exit contract,
  and the runner change. A file that said nothing about its own gaps would be
  the "silent cap" `core/artifact-lifecycle.md` forbids.
- The benchmark itself is armed behind a machine-window watch: at most one
  foreign agent run, ≥5 GB available, memory PSI quiet, held over two checks.
  Release 0 had two runs in flight when it was armed.

## BL-082 — T-14 step 2: the benchmark, and what it took to make it mean anything

- Scoped run against a 35-minute box, four scenarios of eight: **133/134**, and
  the one red is the corpus rather than the edition — the fixture plants no
  defect for the two audit checks v26 added (filed as T-14.1). The four not run
  and the two passes not run are named in `evals/benchmarks/v26.md`; POLICY's
  full-corpus bar is not claimed there.
- **Thrown away once.** The first attempt died at `fresh-scaffold-dotnet` 19/21
  with a hand-ordered `ownedFiles` and an unstamped report. One cause: the arm
  was not on `PATH`, so the agent tried `legislator version`, found nothing and
  did the work by hand. From this edition the law names a binary — a harness
  that does not hand it to the scenario agent measures the machine, not the
  edition. Fixed in `evals-bg.sh` beside the parity variables. `setup_workspace.py`
  was crashing on the same class of leftover (it still copied the deleted engine
  into two fixtures).
- **The model floor is measured, and it is a split.** haiku carries the
  mechanical scenarios (21/21, 25/25 — the log shows `legislator detect/apply/
  verify/report`, one call each, at the four points SKILL.md names) and fails
  the model half: `audit` 45/50, `restructure` 23/37. On sonnet, same law, same
  fixtures, same arm: 49/50 and 38/38, all eighteen failures green. So v26's
  floor is sonnet where a scenario has a semantic half, haiku where it does not
  — and the reading is an experiment, not an argument. Stopping at the haiku
  numbers would have read as an edition regression and cost a day of chasing a
  law defect that does not exist.
- What haiku actually got wrong is worth keeping: it narrated instead of
  executing. `audit` became a hand-written `## Audit Summary` with prose
  appended and no candidates appendix, though the arm had run; `restructure`
  reported six actions as "applied" while `.cursorrules`, the foreign glossary
  and the stray rulebook stayed on disk. Both are the shape a weaker model fails
  in: the deterministic half is fine because a binary does it, and the pinned
  format is where it drifts.

## BL-082 — T-14.1: the two defects the benchmark found in the corpus, closed

- The audit's one red was the corpus, not the edition, and it split in two when
  probed. `case-collisions` was plantable: a copy of an owned rule whose name
  differs by case alone. Probed first — the arm run against the fixture printed
  exactly one collision finding with every other slug's count unmoved — then
  planted with its two report markers, then re-measured on the same model:
  **52/52 clean**. The probe is the point; a plant that quietly trips a second
  check would have been discovered as a mystery failure a week later.
- `arm-integrity` is not plantable and the grader now says so instead of
  pretending. The same probe showed why: from a shell without the binary the
  check answers "`legislator` is not on this machine" — its subject is the
  machine and the edition's release record, neither of which lives in a
  repository fixture. `ENVIRONMENTAL = {"arm-integrity"}` carries the reason and
  the pointer to its real coverage, and the assert fails if someone later plants
  a defect and leaves the exemption standing. Mechanical exclusion by an
  explicit rule, which is what the law asks for; a permanently red assert nobody
  can act on would have been the alternative.
- Found while listing what was still broken rather than by a gate: CI's test
  step ran `dotnet test src` — the exact command T-13.9 proved discovers zero
  tests on this SDK. On a branch that is entirely about the .NET substrate, the
  second arm of the gate was inert. It now runs `check_dotnet.sh`.

## BL-082 — T-14 step 2 closed: the full corpus, and six asserts that measured nothing

- One run, 22:09–23:14, `sonnet`, the whole corpus: **212/212**, eight scenarios
  of eight, no assert regressed. Idempotency **zero ×3** — `upgrade` matters
  most there, its keep list is non-empty and re-serialised byte for byte. The
  total moved 214 → 212 by arithmetic: the retired scenario took six asserts
  out, v25's `tracker-drift` pair and T-14.1's two collision markers put four
  back.
- **The mutation pass paid for itself in its first run.** 206/212, six survived,
  and all six were one defect: the stamp mutations still targeted
  `Emitted by docs/ai/engine.py report`, which this edition renamed. The
  mutation deleted lines that were not there, the report came back unchanged,
  the assert passed — six asserts about the emitter stamp were unfalsifiable and
  would have stayed green forever while measuring nothing. I had re-pointed
  `grade.py`'s stamps in T-13 and missed `mutations.py`'s. Two lines, seven
  seconds to re-run: **212/212 killed**. Worth stating plainly because no amount
  of green history could have surfaced it — that is the whole argument for the
  pass existing.
- Cost of the corpus: ≈ 65 minutes wall, $9.65, `case-practice` and
  `restructure` two thirds of it. `restructure` took 813 s here against 286 s on
  haiku earlier — the same difference between doing the work and describing it
  that its 23/37 showed.
- Recorded in `evals/benchmarks/v26.md`, including what is **not** claimed: the
  absent-arm branch is still not falsified end to end (no scenario can hide the
  binary from itself), and the opencode profile stays frozen.
