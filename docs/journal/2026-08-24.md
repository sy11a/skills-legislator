# 2026-08-24

## BL-034 — self-legislation

Legislated this repository with its own constitution at v20, in migration mode:
real `CLAUDE.md` renamed to `AGENTS.md` and split three ways, thirteen owned
files delivered and byte-verified, OKF bundle seeded, case home created. The
case is filed at `docs/cases/BL-034-self-legislation/` — the first one born
under `core/sdd.md` in this repo rather than back-dated into it.

Decisions taken, both recorded in
[ADR-0002](../adr/0002-the-legislator-repo-is-governed-by-its-own-constitution.md):
the repository is fleet member #0 under a one-way law stratum, and the
knowledge homes the law names win over the ones this repo invented — the
48-term glossary moved forward into `docs/okf/glossary.md`.

Two probes carried the risk. Before delivering anything, the write-guard was
driven over `skill/**` to prove it could not block development of the next
edition; afterwards the same probe showed it flipping to BLOCKED on exactly the
three owned paths and nothing else. Then the hurting case was run for real: a
source rule edited, `skill/VERSION` bumped to 21, delivered copy still holding
v20 bytes, manifest still v20, both commit gates green on the skew.

## Dead ends and corrections

- `R-008` was written too broadly — it forbade every mention of the old
  glossary path, which the case's own record must make to describe the move.
  Amended in the spec with the reason, rather than reworded around.
- The first ambient-context probe of the day (BL-054, earlier) used a fixture
  without the `CLAUDE.md` symlink and produced a false reading. Re-run on a
  correctly shaped fixture; only the second reading was reported.

## Open questions for next time

- **Audit check 2 is wrong in any repo that documents a templating system.**
  Fourteen false Criticals here, every one a token quoted inside backticks in
  prose. The natural fix is to skip inline-code spans. It is a `skill/` change,
  so out of this case's scope — it needs its own edition.
- The permanent delivery channel for this repo is still open: `fleet.sh`'s
  discovery is `-maxdepth 4` and cannot see this path.
- Fleet sweep to v20 ran in parallel today; the three repos upgraded on
  2026-08-23 are still uncommitted, and `fleet.sh status` reports working-tree
  state rather than HEAD, so it called them delivered.
