# 2026-09-02

## BL-082 — stage 6, build: T-09 second half (`audit`), and the ruler that measured Python

(The session that ported `detect` on 09-01 ran on into this day; the audit half
is recorded here because this is the day it was committed.)

The audit job is ported: fourteen mechanical checks of the seventeen slugs the
Python names — the two model checks stay a channel (`--model-findings <json>`)
and `okf-sync-debt` rides beside `okf-anchors` in one pass — and the pinned report
printed from them, sorted by the severity sections and the `AUDIT_ORDER` slug
table, `Clean checks:` naming what found nothing, the stamp verbatim. Twenty
labels answered, all green on the published binary; the engine ruler moved
91 ok / 51 FAIL to 111 / 31, the ledger 116 to 96, the suite 260 to 381 and the
NativeAOT artifact 4 685 560 to 4 839 192 bytes — the startup-budget question is
T-12's, noted for it.

The shape that mattered: the audit is the *second caller* of `anchors` and
`okf-debt`, not a second derivation of them. `AnchorsJob.Unresolved` and
`OkfDebtJob.Stale` were extracted for it, the way `EntryDocument.Of` was extracted
from `detect` and `Prose.Placeholder()` from `sdd-lint` when the audit turned out
to ask the same question — the second-consumer rule applied four times in one
task. `GitLog.Ask` is the one seam every git-backed check goes through, and it
answers `(Output, Available)` so the audit can do what the Python does when git
is not there at all: throw, and let the host print the refusal and exit 2, rather
than report a clean layer over checks that never ran.

Two departures from the plan's letter, for the ownership window. The plan reads
"one test file per check"; checks 1, 2, 4, 6, 14 and 17 have no unit file of their
own — their twins already drive the exact fixture the ruler plants, and a unit test
repeating that would be the H-007 duplicate the case's own research warns of. And
the audit's input was named in the plan as `skill/references/audit-checks.md`,
which does not exist; `skill/SKILL.md` § Audit is the source that does, and the
port reads from it.

The finding of the day is in the ruler, not the port. Six `check_engine.py`
checks hardcoded `sys.executable` where every other check goes through
`_engine_argv`, so on the .NET arm they ran the Python and reported the binary
green. Four are fixed — the three `okf_debt_git_absent` labels and
`engine_audit_fails_loud_without_git` — and proven the cheap way: with
`PARITY_ENGINE_CMD=/bin/false` the four go red where before they stayed green.
The two `usage` labels keep the fault by the T-07 ruling that assigned them to
the CLI task (T-12); the decision-gate stop and its ruling are in the handoff.

Process notes. The red run was captured from the session transcript after the
capture command died on a relative path; the file says so in its first line.
`detect-red.txt` from the first half carried an absolute local path — the
static check was red on a file already committed, which means it ran before the
record was written; redacted to `<repo>` here, and the lesson is that the seven
checks run last, after every record. CA1859 for the seventh time; CS0121 from an
overload pair in a test helper for the second time in the same task — the
`Run(IFileSystem, params)` / `Run(Dictionary)` pair is a shape I should stop
writing.

Open from the handoff and still open: half (a) of the case-sensitivity item — an
audit check for case-collision variants of owned names — was due "by T-09" and is
not in this port. It went back to the operator at the task close and moved to
T-10 as an audit check added beside `apply`, where the owned set is written and
a collision becomes observable; plan Task 10 carries the dated amendment. Both
diffs were owned as built, no correction.
