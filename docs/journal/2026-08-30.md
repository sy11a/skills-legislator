# 2026-08-30

## BL-085 — spike filed: inherit the host's work tracking

The owner added a requirement on top of the v25 pivot: the legislator
must inherit the work-tracking discipline of the repository it legislates
— if the team lives in Jira or a comparable tracker, offer to connect it
(through the tracker's MCP server) and run the case lifecycle there:
creation, numbering, reading. The text backlog becomes the
lowest-priority home, and the legislator always recommends migrating off
it (GitHub Issues at minimum, since every GitHub remote has one). Filed
as spike BL-085 rather than a case: the detection signals and the
tracker × MCP capability matrix are measurements nobody has taken, and
the law wording follows from them. Noted the line to hold whatever the
answer: the tracker owns the *item*, the repository owns the *record*
(case directory, ADRs, journal). Possible absorption of BL-031 recorded.
No code, no `skill/` change — backlog, changelog, journal only.

## BL-082 — the case audited under dev-flow (stages 1–4)

The parked case re-entered through the owner's dev-flow: one collapsed
audit session walked spec, research, ADR-0008 and plan against four
stage exit criteria. Nothing was rewritten; gaps were appended, dated.
Spec: the hurting case got a name (HC-8201); a 2026-08-30 clarification
keeps `Legislator.Parity.Tests` as a fifth test project against
R-8201's letter. Research: §8 receipts the history sweep and the
placement constraints; §2 is amended honestly — the R-8202 props bar is
ABOVE what the dotnet stack law asks of the fleet (only warnings-as-
errors), and fleet-obs is precedent for the props file's existence, not
its content (xUnit v2/VSTest/coverlet, reflection YAML); the toolchain
stays xUnit v3 on MTP + NativeAOT by the owner's ruling, with "promote
the bar into the stack law" as a follow-up candidate. §9 adds what the
ADR does not carry: variant scoring by R-line, the stage-7 refactor
radius, the consumer census, and the owner's new invariant — AOT-first
for every host (publish smoke from the first task, `[GeneratedRegex]`
only, tests exercise the published binary). Design: the plan's twelve
Interfaces blocks moved into `contracts.md` (C-01–C-12, each with its
R-lines, producing task and consumers) after a worked example showed a
contract inside a task dies with the task while BL-083/084 need it as
reference; plan tasks cite `C-NN`. Plan: seven gate amendments applied
in place, the header now says the owner executes under dev-flow stage 6
(the agent sub-skill line is gone). Dead end: an edit script assumed a
contract per task (`C-13`) — twelve contracts, fourteen tasks; its own
assert caught it. Open for stage 5: the edition branch name and the
first commit of these case edits, which sit uncommitted on `master`.
Candidate for the backlog, not filed: `sdd-lint` verifying that plan
tasks cite existing `C-NN` and every contract is cited.

## BL-082 — stage 5, baseline and branch prep

The edition branch `bl/082-dotnet-deterministic-substrate` is cut from
`master` at `85b5053` (PR #36), verified live rather than by convention:
the GitHub default branch, the base of the last ten PRs (#27–#36) and
the merge bases of the two most recent `bl/` branches all say `master`;
`git fetch --prune` found local equal to origin and retired the remote
copy of the merged `bl/082-dotnet-substrate-case`. Two rulings: the
branch name stays as backlog and spec wrote it (branch = case
directory), and this repo works its branches in the single main
checkout — every branch in history did, and `~/.claude/skills/legislator`
symlinks into `skill/` here, so a worktree would detach the live skill.
That second ruling departs from the letter of dev-flow's stage-5 rule
("the main checkout is never touched"); the contested rule is filed to
the dev-flow backlog, not resolved here. Baseline before any change:
`check_static`, `check_engine`, `check_hooks`, `check_opencode_plugin`
(29/29), `engine anchors` and `engine sdd-lint` all exit 0; dotnet
10.0.106 on PATH, no `src/` or `tests/` yet. The case edits from the
stage 1–4 audit (spec, research, plan, contracts.md) become the branch's
first commit. Next: stage 6, task T-01.
