# 2026-09-08 — Tuesday

## BL-082 — converge, first pass: three findings, one of them for the fleet

- Judged against the promises rather than the diff, as `core/sdd.md` requires:
  seventeen R-lines, ADR-0008, the constitutional MUSTs, and HC-8201 — which was
  **executed, not read**: a machine file overriding `cases_dir` to `matters`,
  `config show` answering `cases_dir = matters [machine]`, `sdd-lint` reading the
  overridden directory. The provenance line and the job agree.
- **F-1, and it is the one that matters.** R-8215 promises the hooks fail open
  with one warning while the binary is absent. Measured with `env -i PATH=...`:
  the delivered command line answers `sh: legislator: command not found`, exit
  127 — on every `Edit`, `Write`, `Bash` and `Stop`, forever, on any machine
  without the arm. The *open* half survives only because 127 is not 2 and the
  harness tolerates it; the *one warning* half is simply gone. Up to v25 the
  launcher was a shim ending in `exit 0`, silent by construction; v26's bare
  command dropped the property without replacing it. This is the only finding
  that reaches someone who never opens this repository, and the case cannot
  close on it — it is the operator's ruling.
- **F-2**, CRITICAL by the letter: the audit's exit contract changed (Info no
  longer raises it) on a decision-gate stop the operator resolved, and no ADR was
  written. `core/adr.md` says same task, not later. Written now as ADR-0010, with
  the three options weighed and the consequence recorded — the pinned clean shape
  is the absence of the actionable sections plus exit 0, not the presence of
  `No findings.`.
- **F-3**: `docs/ontology.md` still described the Python engine in three places.
  Worth more than the fix: it survived because it sits *outside* the anchored
  bundle — `index.md`, `codebase-map.md` and the concept documents are what
  `legislator anchors` reaches, and the deeper narrative the index links to is
  not. The reference document most likely to rot is the one the rung cannot see,
  and this repository has exactly one of those.
