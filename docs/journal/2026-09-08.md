# 2026-09-08

## The queue moves to Architector (BL-093)

Architector's case BL-011 reviewed every governed backlog against the release-cycle concept; the operator ruled (Architector ADR 0009) that a product backlog holds only the running release's tasks and that the fleet's idea and pre-release backlogs live in Architector's tracker, and asked for this repository's queue to be clean after v26. The "Agreed order after v24" roadmap became a pointer table; sixteen open sections (BL-008, 039, 040, 046, 074, 076, 077, 078, 079, 080, 081, 083, 084, 086, 090, 092) carry a moved-to line naming their Architector item; eleven carry a closed line — BL-025, 026, 059, 060, 089 delivered, BL-010 moot, BL-031, 067, 091 and the two notes absorbed. BL-088 (Release 0's L-1) stays, the case register stays as history. Documents only; no law, no code.

## L-1 — spec and research (stages 0–2, dev-flow feature flow)

- The case opened in-repo: `docs/cases/L-1-law-reads-by-mode/spec.md` carries the frozen
  contract verbatim (story S-5.2, scenarios A-5.2.1/A-5.2.2, R-001…R-006), tier 1, the
  decision policy (DP-1…DP-5) from the kernel's launch line, and the worked clarifications
  (check 21, the fixture triplet, no ENVIRONMENTAL cover). Branch `l/1-law-reads-by-mode`
  was cut from the live `origin/master` tip `88205b9` (PR #44 merged before the cut; the
  launch line's own live-remote-wins rule resolved it).
- Research (stage 2) swept the history and the code: waterflow is a settled concept with no
  code anywhere — only backlog row BL-088, Architector ADR 0007 (proposed) and
  `release-cycle.md`. The whole scaffold for check 21 is the check-18 quartet: the entry
  read (`EntryDocument.Of` + `Read` with substring markers), the `Order`/`Place`
  registration, the fixture triplet (`report_markers`, `check_slugs_covered`, the planted
  defect; grade.py's parity reddens without any one), and the quiet case at the twin
  boundary (`check_engine.py` labels + `[Parity]` twins, ledger at 0). Findings recorded in
  `research.md`: BL-088's "and its kernel" half is out of scope (the frozen contract names
  only the release-branch convention — DP-2), and the fixture's mode line must not trip
  check 11 once the law states both modes. Design (stage 3) chooses the template wording,
  the finding text and the options member names; the slug is pinned 21 `waterflow-mode`,
  severity Warning, full e2e benchmark warranted, VERSION bump proposed but never decided
  (DP-1).

## L-1 — design (stage 3, dev-flow feature flow)

- Design closed 2026-09-08 with `docs/cases/L-1-law-reads-by-mode/design.md` and
  ADR-0012. The decision, taken under the pre-written policy (DP-1…DP-5, autoflow
  reading of the `[operator]` step): the check-18 quartet in full as the detection
  mechanism — closed-token substring markers, both markers new options members
  (`WaterflowModeMarker` = "Development law mode: waterflow", `ReleaseBranchMarker` =
  "Release branch:"); the tpl gains `- Development law mode: pair` beside the `Task
  tracker:` line; the finding is a Warning naming the entry verbatim; the law's Mode
  preamble keeps the pair reading exactly as today's text with each cornerstone's
  waterflow reading beside it. Rejected with reasons: manifest-declared mode (the frozen
  contract names the entry document), model-judged mode (a repository-fact check is
  mechanical; cannot be planted), parsed line model (over-engineering against the closed
  convention). ADR written (accepted — the ADR-0009 precedent): the decision outlives
  the case; it carries the feature boundaries (kernel half out, no VERSION bump, no
  fleet delivery) and the consumer census for stage 8. Contracts C-1…C-9 in design.md
  are what the plan cites. Nothing parked, no prototype needed; branch still clean at
  `88205b9` plus the case artifacts.

## L-1 — plan (stage 4, dev-flow feature flow)

- Plan closed 2026-09-08: `docs/cases/L-1-law-reads-by-mode/plan.md` cuts the design
  into 8 one-session tasks (T-01…T-08), all R-001…R-006 traced, no placeholders, `[P]`
  groups T-01/T-02/T-03 and T-06/T-07. Two strict joints: T-04 (the test quartet, C-6)
  precedes T-05 (check 21 + SKILL.md registration, C-4/C-5) — T-04's commit is the
  plan's one expected-red commit, the red-first demonstration `.claude/rules/evals.md`
  demands (the label ledger ratchet carries it); T-06 (fixture, C-7) needs T-03 (law
  states both modes → the planted declaration contradicts nothing, check 11) and T-05
  (the slug in SKILL.md); T-08 (the e2e benchmark) runs last, sized from v26's record
  (≈65 min, ≈9.7 USD) and recorded as `evals/benchmarks/v27.md` — a proposal, never a
  VERSION decision (DP-1).
- Two findings the plan made along the way, both countered by the policy rather than
  re-decided: the every-commit gate needs a binary and none is on this machine — the
  build must publish first (`sh tools/publish-legislator.sh`, git-ignored `artifacts/`,
  `release.json` untouched, its empty digests keep check 20 on its Info line); and
  adding options members requires the `OptionsComposer` `Apply` branches too, or the
  settability census throws (C-04). The sdd-lint mechanical pass ran as the explicit
  checklist `core/sdd.md` prescribes when the arm is absent — walked and recorded in
  the plan; the binary's rung closes at T-05 and runs before done. The analyze gates
  passed with no findings: reuse-first (the check-18 quartet is the whole scaffold,
  research §2) and over-engineering (V-B/V-C/V-D and the kernel half already rejected
  at design; nothing unrequested builds). Tracker `## Tasks` seeded from the plan; no
  stage has committed — commits happen per plan task at build. Next: baseline (stage
  5).

## L-1 — baseline (stage 5, dev-flow feature flow)

- Base-branch census (mandatory live check, pattern anchor A-004): convention verified
  against the live remote — `gh repo view` says the default branch is `master`, and
  `gh pr list` shows 10/10 PRs targeting `master` (the recorded convention holds). The
  freshness census did not: two PRs merged after the branch cut. PR #45
  (`fix/ci-runs-the-gate-with-bash`) advanced `origin/master` from `88205b9` to
  `bd7d31e` — it changed the very gates the build runs (`evals/check_static.py` gains a
  guard that fails on any live caller of `evals/check_dotnet.sh` through `sh`;
  `.claude/rules/dotnet-substrate.md` now demands `bash`). PR #46 (BL-093, the backlog
  move) then advanced it to `4733379`, replacing the day's journal with its own entry.
  Under the operator's pre-written rule (spec Clarifications Q:base — the live
  origin/master tip; the launch line's live-remote-wins precedence, already applied once
  at triage when `1c260b7` became `88205b9`), the branch fast-forwarded twice to the
  fresh tip `4733379`; the L-1 journal sections were re-spliced on top, and PR #45's
  removed journal section was not resurrected (the arbiter's own rewrite wins). Recorded
  as a Route deviation in the tracker.
- Worktree census: three worktrees — the main checkout `custom_skills/legislator` on
  `master` (untouched, clean), `legislator-B11` (BL-093's branch), and this task's
  `legislator-E` worktree on `l/1-law-reads-by-mode`. The branch is named per the
  linked convention (`l/1-<slug>`, the Release-0 key following the `bl/NNN-<slug>`
  shape).
- Baseline run, all gates green on the final fresh base `4733379`: `python3
  evals/check_static.py` (incl. PR #45's new `sh`-guard) · `python3 evals/check_engine.py`
  · `python3 evals/check_hooks.py` · `node evals/check_opencode_plugin.mjs` (29/29) ·
  `bash evals/check_dotnet.sh` (657 .NET tests, strict build, AOT publish smoke) ·
  `legislator anchors` and `legislator sdd-lint` exit 0 — the plan-time sdd-lint gap
  (binary absent) is closed by the published arm.
- Pipeline quirk discovered and recorded: the parity rulers' documented usage form
  (`PARITY_ENGINE_CMD=artifacts/<rid>/legislator`) is a relative path that fails —
  `check_engine` runs its jobs from a temp workspace cwd (`FileNotFoundError`), and
  `check_hooks` symlinks the relative path into its fixture, making the arm a dangling
  symlink whose guard then fails open silently (exit 0 — measured green by the R-8215
  half). Both rulers pass only with an absolute binary path. The rulers' usage lines
  and the sources note carry the relative form; corrected by absolute path in this
  run.
- The arm: published `artifacts/linux-x64/legislator` (26.0.0) via
  `tools/publish-legislator.sh`; `release.json` untouched (empty digests keep check 20
  on its Info line). Working tree carries only the task's own artifacts: the journal
  sections, `docs/adr/0012-…`, `docs/cases/L-1-law-reads-by-mode/`. Main checkout
  `custom_skills/legislator` pristine on `master`. Start state is honest: green on the
  fresh tip, so a later red of Test Design is attributable to our change.

## L-1 — T-01 (build, dev-flow feature flow)

- T-01 closed: the two markers became `LegislatorOptions` members (design C-2/C-3,
  per R-003/R-004). `WaterflowModeMarker` = "Development law mode: waterflow" and
  `ReleaseBranchMarker` = "Release branch:", each with its KeyMap row
  (`waterflow_mode_marker` / `release_branch_marker`), its Enumerate row and its
  `OptionsComposer.Apply` branch (C-04: a member in KeyMap but not applied is an
  exception the settability census throws). Four mirror lists, one position each,
  following the `LegislationMarker` in-class precedent. The case record from stages
  0-5 (spec/research/design/plan, ADR-0012, the journal's stage sections) was tidied
  into its own commit first (`cf84e19`), so T-01's commit carries only its diff.
- Red, observed and recorded (predicted: the two settability rows fail naming the
  missing branch; green siblings hold): `Every_key_is_settable_from_a_layer` failed
  on `waterflow_mode_marker` and `release_branch_marker` with "WaterflowModeMarker is
  in KeyMap but not applied" / "ReleaseBranchMarker is in KeyMap but not applied"
  (OptionsComposer.cs:132), the Census's C-04 contract; 114 of 116 Core tests green
  at red. New asserts: none - T-04 is the plan's new-assert task, and the census
  rows arrive with their members.
- Green: the two `Apply` branches; `bash evals/check_dotnet.sh` "all 659 .NET tests
  passed" (657 baseline + the two new census rows), AOT publish smoke clean;
  `python3 evals/check_static.py` "all static checks passed" (R-8209 — the marker
  literals live in the options model, their one lawful home); both parity rulers
  green on the re-published arm with absolute `PARITY_ENGINE_CMD`/`PARITY_HOOK_CMD`
  (the stage-5 quirk, `artifacts/linux-x64/legislator`); opencode plugin 29/29;
  `legislator anchors` and `legislator sdd-lint` exit 0. Nothing reads the markers
  yet — check 21 lands at T-05.

## L-1 — T-02 (build, dev-flow feature flow)

- T-02 closed: `skill/assets/templates/AGENTS.md.tpl` gains `- Development law
  mode: pair` immediately after the `Task tracker:` line (design C-1, wording
  W-1, per R-001). One line, no other template change; a `waterflow` repository
  changes the value and adds `- Release branch: <name>` on its own word. The
  pair value is the template's declared default — a repository declaring
  nothing reads as `pair` (R-005's quiet half starts here).
- Red, per task (test-design discipline, applied to this task alone): no new
  assert and no expected red — the plan's one expected-red commit is T-04; the
  driver is the existing tpl import-wiring assert (`check_static.py`, every
  core rule `@`-imported) which must stay green, and the fresh-scaffold proof
  lands at T-08 (verifiability rung e2e, deferred by the plan). Coupling note:
  the line is coupled to C-1's exact wording and shares the closed prefix
  `Development law mode:` with C-2's marker; nothing in-tree reads the tpl's
  mode line yet — grade.py derives migration wiring from `^@` import lines
  only, and check 21 reads a repository's entry text, never the tpl. Predicted
  colors: every gate green before and after; matched.
- Green: `python3 evals/check_static.py` "all static checks passed" (incl. the
  tpl import-wiring block, all 11 core rules); `python3 evals/check_engine.py`
  and `python3 evals/check_hooks.py` green on the absolute parity paths (the
  stage-5 quirk: `PARITY_ENGINE_CMD`/`PARITY_HOOK_CMD` = `artifacts/linux-x64/legislator`);
  `node evals/check_opencode_plugin.mjs` 29/29; `bash evals/check_dotnet.sh`
  "all 659 .NET tests passed" (unchanged count — T-02 touches no code), AOT
  publish smoke clean; `legislator anchors` and `legislator sdd-lint` exit 0
  through the re-published arm. Siblings unmoved, full relevant suite clean.

## L-1 — T-03 (build, dev-flow feature flow)

- T-03 closed: `skill/assets/rules/core/pair-development.md` states both modes (design C-8, per R-001/R-002/R-006). The Mode preamble names `pair` as the default (reading exactly as the bullets below) and `waterflow`; absent a declaration the law reads as `pair`. The three cornerstone rules each carry their `waterflow` reading as a sub-bullet beside today's text, worded after the frozen contract's table (Architector ADR 0007): one task per track, tracks in parallel; never merge to `master`, the release kernel merges into the release branch on the reviewer's and the gates' green; approvals are per release - the plan approval and the release-contract approval - and inside the release the decision policy rules. A closing line states the integration-branch merge stays the operator's in both modes. The pair bullets are byte-identical to today's text (R-001); only the law source moved - `docs/ai/rules/**` is untouched (R-006, delivered-copy hash unchanged).
- Red, per task (test-design discipline applied to this task alone): no new assert and no expected red - the plan's one expected-red commit is T-04. The drivers are the every-commit gates staying green and the R-006 byte-identity grep. Predicted: all green before and after; matched. Coupling note: the added text is coupled to C-8's shape; no in-tree gate parses the law's bullet structure (check_static checks the heading and the tpl import rows; the guard hook cites the file in comments only); the check-11 interplay (a `waterflow` declaration contradicts nothing once the law states both modes) is deferred to T-06/T-08 per the plan (verifiability rung recorded).
- Green: `python3 evals/check_static.py` all static checks passed (incl. the rule-files and tpl import-wiring blocks); both parity rulers green on the absolute parity paths (the stage-5 quirk); `node evals/check_opencode_plugin.mjs` 29/29; `bash evals/check_dotnet.sh` all 659 .NET tests passed (unchanged count), AOT publish smoke clean; `legislator anchors` and `legislator sdd-lint` exit 0 through the re-published arm.
- Ownership approved under the autoflow reading (no operator window): the diff is the C-8 shape with nothing unrequested; ledger and handoff carry the ruling. One drafting excess was cut during green (a parenthetical on the closing line the design never asked for).

## L-1 — T-04 (build, dev-flow feature flow)

- T-04 closed: the test quartet arrives red first (design C-6, per R-003/R-004/R-005) - the plan's one expected-red commit. `evals/check_engine.py` gains three ruler labels in the check-18 region (`audit_check21_waterflow_without_convention` - reports `[waterflow-mode]` and the entry name; `audit_check21_quiet_with_convention` - silent; `audit_check21_quiet_pair` - silent), each with its own `audit_repo` entry file carrying the mode line (the closed substrings T-01's members name, C-2/C-3). `tests/Legislator.Parity.Tests/Engine/AuditTwins.cs` gains the three `[Parity("engine", label)]` twins after check-18's triple, with the same entry files. `LabelCoverageTests` holds at debt 0 - labels arrive with their twins, the ratchet's contract.
- Red, observed and recorded (predicted: the without-convention half fails because `[waterflow-mode]` appears in no output; the two quiet labels pass trivially against an absent check and guard the false-positive half; all siblings unmoved). Preparation: `tools/publish-legislator.sh` published the arm from the current source (v26 - check 21 does not exist in it; `artifacts/` is git-ignored, `release.json` untouched; `check_dotnet.sh` does not publish on a red suite, so the arm stays the pre-published one). `PARITY_ENGINE_CMD=artifacts/linux-x64/legislator python3 evals/check_engine.py`, verbatim: `  FAIL  audit_check21_waterflow_without_convention — out="# AI-Layer Audit — audit-eval-…, 2026-09-08 …` - the clean-checks line reads `Clean checks: imports-resolve, …, tracker-drift, case-collisions` with no `waterflow-mode` in it; the report is clean and exits 0, one check(s) FAILED. `bash evals/check_dotnet.sh`, verbatim: `failed Legislator.Parity.Tests.Engine.AuditTwins.Audit_check21_waterflow_without_convention (4ms)` - `Assert.Contains() Failure: Not found: "[waterflow-mode]"` (AuditTwins.cs:441); 1 of 206 Parity tests failed, every other module green.
- Green siblings unmoved: `python3 evals/check_static.py` all static checks passed (R-8209's literal scan reads `src/` only - the marker strings appear in `evals/` and `tests/`, outside it); `PARITY_HOOK_CMD=artifacts/linux-x64/legislator python3 evals/check_hooks.py` all hook checks passed; `node evals/check_opencode_plugin.mjs` 29/29; `legislator anchors` and `legislator sdd-lint` exit 0. The red is confined to the quartet; T-05 turns it green.
- Coupling note: the quartet is coupled to C-6's exact markers - each entry file's lines are the closed substrings T-01's options members carry (C-2/C-3), and the labels name the slug the SKILL.md / `AuditChecks.Order` parity pins at T-05. The "declares nothing" half of R-005 rides the existing clean-audit labels (`audit_clean_repo_clean_report`, `audit_untagged_edition_is_an_info_line_not_a_finding`) - a misfiring check 21 would fail them. The mode line trips no check-11 interplay in these fixtures (no `.claude/rules/` files to scan; research §3's note is about the T-06 fixture entry).
- Ownership approved under the autoflow reading (no operator window): the diff is exactly the C-6 quartet - three ruler labels, three twins, their entry files - with nothing unrequested; ledger and handoff carry the ruling.

## L-1 — T-05 (build, dev-flow feature flow)

- T-05 closed: check 21 lands (design C-4/C-5, per R-003/R-004/R-005) - the engine method and its registration, greening T-04's quartet. `src/Legislator.Engine/Audit/AuditChecks.cs`: `Order` gains `waterflow-mode` at position 21 after `arm-integrity`; `Run()` gains `Check21WaterflowMode(entry)` after check 20. The method reads the entry text once through check 18's tolerant read (IO failure → empty text, no false finding), matches the two closed markers T-01's options members carry (`options.WaterflowModeMarker.Value` / `options.ReleaseBranchMarker.Value` - no literal in the check, R-8209), and yields the C-5 finding - a Warning naming the entry verbatim - only when the waterflow marker is present and the convention marker is absent. Waterflow with its convention, pair, and no declaration are all silent (R-004/R-005); a null entry is silent. `skill/SKILL.md` § Audit: the pinned-slug line gains `21 waterflow-mode` and a numbered item 21 after check 20 spelling the behaviour and the C-5 template - both arms hold the same slug.
- Red, observed and recorded (predicted: the without-convention half fails on both arms; the two quiet labels pass trivially and guard the false-positive half; siblings unmoved). Before the change, re-run on the v26 arm (check 21 does not exist in it; `PARITY_ENGINE_CMD=artifacts/linux-x64/legislator python3 evals/check_engine.py`), verbatim: `FAIL audit_check21_waterflow_without_convention` - the clean-checks line reads `Clean checks: imports-resolve, ..., tracker-drift, case-collisions` with no `waterflow-mode`, byte-matching T-04's recorded red; `1 check(s) FAILED`, confined to the quartet. The .NET twin's red stays as recorded at T-04 (tree and arm unchanged since that commit).
- Green: `tools/publish-legislator.sh` re-published the arm from the current source (check 21 in it; `artifacts/` git-ignored; `release.json` untouched, empty digests keep check 20 on its Info line). Engine ruler: all three quartet labels green (without-convention now reports `[waterflow-mode]` and the entry; the two quiet labels hold). Direct probe on a waterflow-without-convention repo, the finding line verbatim: `- [waterflow-mode] AGENTS.md: declares the waterflow mode but names no release branch → add a line naming it, e.g. "- Release branch: release/0"` - C-5's template with the entry named verbatim. `bash evals/check_dotnet.sh`: all 662 .NET tests passed (659 baseline + the three twins, `LabelCoverageTests` at 0 - the ratchet), AOT publish smoke clean. `python3 evals/check_static.py` all static checks passed (R-8209: the check holds no marker literal); `PARITY_HOOK_CMD=artifacts/linux-x64/legislator python3 evals/check_hooks.py` all hook checks passed; `node evals/check_opencode_plugin.mjs` 29/29; `legislator anchors` and `legislator sdd-lint` exit 0 through the published arm.
- Coupling note: check 21 is coupled to C-4/C-5's exact markers (the options members T-01 added - if their strings changed, the check and the quartet move together), the single C-5 finding line, and the pinned slug shared with `AuditChecks.Order` (grade.py's parity derivation reads the SKILL.md slug side at T-06; the label ledger ratchets at 0). The "declares nothing" half of R-005 rides the clean-audit labels; a misfiring check 21 would fail them - it did not.
- Ownership approved under the autoflow reading (no operator window): the diff is exactly C-4/C-5 - one Order row at 21, one Run() call, the ~20-line method, the slug line and item 21 in SKILL.md - with nothing unrequested; no VERSION bump, no `docs/ai/**` edit, no release.json touch. Ledger and handoff carry the ruling.

## L-1 — T-06 (build, dev-flow feature flow)

- T-06 closed: the fixture triplet lands (design C-7, per R-003/R-004) - the plant and its meta rows live in `evals/setup_workspace.py` only. The rotted-layer entry (`CLAUDE.md`) is written with `- Development law mode: waterflow` (next to the Domain glossary bullet, where the template's instance-data block carries the mode line) and no `- Release branch:` line. `report_markers` gains the two order-independent markers after defect 17's pair - `"waterflow-mode]"` (defect 21a: the pinned slug) and `"CLAUDE.md"` (defect 21b: the entry named verbatim) - under a comment citing defect 15's hidden lesson (the finding line names slug and entry separately; their order is never pinned). `check_slugs_covered` gains `"waterflow-mode"` after `"case-collisions"`. `grade.py` is untouched - it derives `law_slugs` from SKILL.md's pinned-slug line (the slug landed there at T-05) and reddens on any one missing; no `ENVIRONMENTAL` cover (closed for repo-fact checks). The quiet-with-convention half stays at the twin boundary (D-5); the e2e proof is T-08's audit scenario.
- Red, observed and recorded (predicted: the meta-assert reddens without the plant - the fixture's report marker depends on check 21 firing). The scratch workspace was materialized under the operator-ruled cache home (the kernel addendum of 2026-09-08 12:30: never /tmp; the plan's /tmp form substituted by that ruling). Probe A (plant in): the published arm's audit on the materialized rotted-layer repo, the finding line verbatim - `- [waterflow-mode] CLAUDE.md: declares the waterflow mode but names no release branch → add a line naming it, e.g. "- Release branch: release/0"` - C-5's template, both order-independent markers present, and the mechanical half reports no finding naming the mode line (the model-judged check-11 interplay is T-08's, where the model runs). Probe B (plant out, on a mutated copy of the materialized repo): check 21 goes silent, the report holds no `waterflow-mode]` - only the bare slug on the Clean checks line - so the report-marker assert for defect 21a reddens; the bracket is what keeps the marker from being satisfied by the quiet spelling (the same design lesson as defect 15, found 2026-08-23).
- Green: the workspace materialized clean (eight scenario repos); the materialized meta carries the three rows; the derivation selftest on the workspace graded 16/16 (`audit_slugs_derived` holds - waterflow-mode is one pinned slug per a severity-carrying check, both sides of the parity equation in their homes). `python3 evals/check_static.py` all static checks passed; both parity rulers green on the absolute parity paths (the stage-5 quirk); `node evals/check_opencode_plugin.mjs` 29/29; `bash evals/check_dotnet.sh` all 662 .NET tests passed (unchanged count - the tree carries no src/ change), AOT publish smoke clean; `legislator anchors` and `legislator sdd-lint` exit 0 through the published arm.
- Coupling note: the three rows are coupled to C-7's exact markers - the plant line is the closed substring C-2's member names, the finding line is C-5's template, and the slug row must match SKILL.md's pinned slug (grade.py's parity derivation reads the SKILL.md side; a renamed check without its row here is red at grade time, and a row for a check the law does not pin is orphaned the same way). No VERSION bump, no `docs/ai/**` edit, `release.json` untouched, `docs/ai/manifest.json` untouched.
- Ownership approved under the autoflow reading (no operator window): the diff is exactly the C-7 triplet - one mode line in the plant, two report markers, one slug row, their comments - with nothing unrequested; ledger and handoff carry the ruling.

## L-1 — T-07 (build, dev-flow feature flow)

- T-07 closed: the documentation homes (design C-9, per R-001/R-006 and the okf.md MUSTs). `docs/okf/glossary.md` gains the three rows the mode concept mints — `mode` (home: the named reading of the development law a repository's entry document declares, `pair` the default or `waterflow`; absent a declaration the law reads as `pair`), `waterflow` (coin: one task per track with tracks in parallel, the release kernel merging into the release branch on the reviewer's and the gates' green, approvals per release) and `release branch` (industry: the branch a `waterflow` repository names in its entry document as its release-branch convention, the naming check 21 requires) — definitions drafted in `design.md` §4, appended after `startup budget` in the table's edit-order convention. The Lives cells name this repo's own files and Architector ADR 0007 by name (records discipline: no fleet names, no absolute paths). `docs/okf/index.md`'s bundle mapping was verified: the "A new domain term … → glossary.md" row already routes the new rows, so no index edit was needed — verified, not skipped. `docs/okf/log.md` gains the entry; `CHANGELOG.md` `[Unreleased]` gains the Added line covering both modes, the template line, check 21 and the fixture. This section closes the case's documentation set.
- Red, per task (test-design discipline applied to this task alone): no new assert and no expected red — the plan's one expected-red commit was T-04; a docs task's drivers are the every-commit gates staying green and the anchors/sdd-lint rungs reading the new rows (glossary and log are the human-class exceptions, so anchors do not bind them, and the lints judge the OKF front-matter statuses, the changelog's `[Unreleased]` and the journal's day-names). Predicted: all five gates and both rungs green before and after; matched.
- Green: `python3 evals/check_static.py` all static checks passed (the records wall: the new lines carry no absolute path and no fleet name); both parity rulers green on the absolute parity paths (`PARITY_ENGINE_CMD`/`PARITY_HOOK_CMD` = `artifacts/linux-x64/legislator`, the stage-5 quirk); `node evals/check_opencode_plugin.mjs` 29/29; `bash evals/check_dotnet.sh` all 662 .NET tests passed (unchanged count — docs-only), AOT publish smoke clean; `legislator anchors` and `legislator sdd-lint` exit 0 through the published arm. `docs/ai/rules/**` byte-untouched (R-006).
- Coupling note: the three rows are coupled to C-9's wording and the frozen contract's vocabulary — `mode`'s definition names the closed values C-1/C-2 pin (the pair value, the waterflow value) and `release branch`'s Lives names check 21's slug; nothing mechanical binds the glossary to the contracts (human class, no anchor), so the coupling holds by review, not by gate. The check-11 interplay stays T-08's (the model-judged half; the law now states both modes, so the fixture's declaration contradicts nothing — research §3).
- Ownership approved under the autoflow reading (no operator window): the diff is exactly the C-9 set — three glossary rows, one OKF log entry, one changelog Added line, this journal section — with nothing unrequested; ledger and handoff carry the ruling.

## L-1 — T-08 (build, dev-flow feature flow) — resume, warrant decision

- Resume context (A-30, revisited by the workspace record): a full corpus run of this exact HEAD
  was already executed at 13:05 (law commit `88a5514`) before the host's low-memory guard cost the
  session — the audit scenario (the hurting case) graded 54/54 clean with the check-21 finding and
  no project-rules false positive on the mode line, idempotency ×3 was zero-diff, and the one
  `restructure` flake recovered clean on its documented re-run. The kernel's stall probe watches
  commits and the repo worktree only, so the scratch-only activity read as a stall; the record is
  the arbiter. What that run never completed: the mutation pass and the benchmark record.
- Warrant decision (DP-2, A-30 rule 3): the existing workspace cannot carry the record. My own
  wrong-environment re-grades overwrote five `grading.json` baselines and appended false rows to
  the append-only grade histories, and a killed mutation pass left a replacement token inside a
  fixture, so the preserved marker no longer resolves on the tree. A record written from this
  workspace would describe a tree it did not make. Therefore a fresh full run is warranted — the
  split is not a silent skip nor a silent re-run; it is stated here. A clean workspace, one tracked
  run (static → smoke → corpus → idempotency ×3), then the mutation pass, then the record.
