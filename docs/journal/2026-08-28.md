# 2026-08-28

## Release v23 — tag, sweep, fleet MRs

Tagged `v23` on the merge commit and swept the fleet: 8/8 ok, nothing
committed by the sweep (contract), then — on the owner's instruction —
committed the four-file delivery on `feature/legislator-v23` in every
repo and opened six MRs (two repos have no remote: local branches
only). A ninth legislated repo (`kbl`) appeared already at v23 — the
owner's own run on the live skill. Two review items surfaced by the
sweep agents for the owner: fleet-agent's AGENTS.md still points
specs/plans at `docs/superpowers/` (check-11 conflict, untouched), and
ProxyController carries five stale OKF anchors. Also: a stray tracked
pyc — my earlier ignore fix had only covered `evals/` — cleaned in PR #30.

## BL-048 — per-job model floor (spike, tier 1)

The last spike before v24. Clarified candidates (`qwen2.5:3b`,
`llama3.2:3b`, `haiku`) against a 15-statement set whose every label
traces to a grader expectation or fixture disposition. The result is
clean and negative for the judgement job: the two locals collapse to
constant answers — one all-NOT, the other proposing even the
marker-suppressed line as fleet law — and haiku's 80% is still three
wrong proposed laws per fifteen. The one positive (glossary extraction,
100% recall for all three, sub-second locally) is filed as BL-074 for
measurement at scale; the honest note is that the evidence is two terms
from one text. Dead end: a first probe run overwrote its own results file
(runner fixed to merge). Open: BL-074's larger set; the queue now points
at v24.

## BL-073 — the eval workspace lock (tier 1, bugfix)

The instrument repair before v24, on the same logic as BL-061/056 before
the v22 sweep: v24 is the next cycle to run the corpus and the mutation
pass on one workspace, which is exactly where v23 lost data twice. One
lock in `tools/proc.py` (create-exclusive `.lock`, holder record, pid
liveness — never age), taken by `evals-bg.sh` before its first write and
by `mutate.py` around the pass. Clarified with the owner: a dead holder
is taken over automatically but loudly; `grade.py` stays lock-free.
Along the way the runner's provenance/cleanup block moved below its
function definitions (it now needs `scenario_dirs`), which also let the
cleanup honour `--skip-smoke`. Red-first: 5 discriminating FAILs shown
against the unchanged harness, then green; a live probe of the stale
path through the real runner confirmed the takeover line lands in
`status.md` and the lock is gone after the gate failure. Found and fixed
on the way: a fleet repo name in this journal's morning entry had slipped
past me — `check_static` caught it. Next: v24 (engine apply/verify +
Step-7/restructure emitters).

## BL-075 — edition v24 opened and built (tier 2)

The queue's next item. Clarified with the owner at opening: the slice is
apply/verify + detect + the Step-7 emitter (restructure emitter and
fidelity job filed as BL-076 for v25), and the engine may write the owned
layer — ADR-0006 extends ADR-0003's sentence with one clause. Built
red-first: 36 engine units shown FAIL (unknown job, exit 2) before a line
of the jobs existed, then green; the five new corpus asserts shown red
against a copy of the v23 workspace (stamp ×4, the scaffold probe; the
re-print match unmeasured by construction — no record exists in v23; the
two Created/Deleted asserts green as the spec predicts, mutation-evidenced).
Decisions worth remembering: the run record lives OUTSIDE the repo at a
path derived from the root — inside would break idempotency's zero-diff,
and a harness-supplied path would restate law in the prompt; report lines
are closed-phrase facts, not prose. A manual end-to-end run on the upgrade
fixture surfaced one emitter defect (a renamed entry document also listed
as scaffolded) — fixed red-first. Open: the benchmark cycle (owner's go),
then member-#0 delivery through `engine apply` itself.
