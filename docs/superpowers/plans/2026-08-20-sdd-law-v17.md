# SDD law (constitution v17) — implementation plan

Case: BL-032 · Spec: `2026-08-20-sdd-law-v17-design.md` · Branch:
`feature/bl-032-sdd-law-v17` · Every phase ends green before the next.

## Phase 1 — Law (assets/rules/**, VERSION untouched until P4)

- [ ] 1.1 Write `skill/assets/rules/core/sdd.md` — the ten law clusters
      from the spec as imperative bullets; `## SDD Practice Rule` H2
      opener; no how-to (pointer to the case README template).
- [ ] 1.2 Amend `core/artifact-lifecycle.md`: `docs/cases/` in the
      conventional homes; the **generated** role class bullet.
- [ ] 1.3 Amend `core/skills.md`: mapped-but-uninstalled fallback line.
- [ ] 1.4 `check_static.py` green.

## Phase 2 — Delivery surface (SKILL.md, templates)

- [ ] 2.1 `cases-README.md.tpl` — create-once case home README
      (what/where, tier line, type header, link to the law; no
      `{{TOKENS}}` beyond none — static file).
- [ ] 2.2 SKILL.md Step 4: add the `docs/cases/README.md` row
      (create-once template); drop the two `docs/superpowers/*` empty-dir
      rows from fresh scaffold; keep Step 5 relocation targets as-is.
- [ ] 2.3 SKILL.md scan-set edits: harvest do-not-scan + `docs/cases/**`;
      check 7 exemption + `docs/cases/**`; check 12 exclusion +
      `docs/cases/**`.
- [ ] 2.4 BL-028 manifest key: `profiles` → `stacks` in Step 1
      (fallback read), Step 3.5 deletion logic, Step 3.7 serialization
      (key rename, order unchanged), edge-case reconstruction, and all
      prose incl. "stack profile" compounds.
- [ ] 2.5 BL-030 sweep: every "constitution file"/loose usage in
      SKILL.md + `references/*.md` → AGENTS.md / the rules corpus.
- [ ] 2.6 `check_static.py` green (template wiring).

## Phase 3 — Evals (rides included)

- [ ] 3.1 `grade.py`: SCAFFOLD_ARTIFACTS + cases README; manifest
      asserts `stacks` key (not `profiles`); severity-anchored Critical
      assertions (BL-025.2); restructure skill-binding scoping to
      `For the team:` (BL-025.6).
- [ ] 3.2 `setup_workspace.py`: upgrade fixture plants a legacy
      `profiles` manifest (graded migration to `stacks`).
- [ ] 3.3 `tools/link-skills.sh`: `mkdir -p "$DST"`; link mode exits
      non-zero on MISSING-SOURCE drift (BL-025.4).
- [ ] 3.4 VERSION 16 → 17.

## Phase 4 — Benchmark (per evals/README)

- [ ] 4.1 `setup_workspace.py /tmp/legislator-eval-v17`; run 5 scenarios
      (fresh, migration, upgrade, audit, restructure) — fresh agents,
      pre-approved `dotnet`, no commits; save reports to `outputs/`.
- [ ] 4.2 `grade.py /tmp/legislator-eval-v17` — investigate any red
      (nondeterminism caveat: re-run 2–3× before conclusions).
- [ ] 4.3 Idempotency ×3 (fresh, upgrade, restructure) — zero diffs.
- [ ] 4.4 Record `benchmarks/v17.md` vs v16 (pass rate, tokens, wall,
      idempotency) + BL-025.1 heading-pin observation from the migration
      runs.

## Phase 5 — Close

- [ ] 5.1 Backlog: BL-032 DONE (with benchmark pointer); BL-028/030
      done riding; BL-029 already absorbed; BL-025 triage recorded
      (rides shipped, item 3 excluded-stale); BL-026 order advances.
- [ ] 5.2 CHANGELOG-style note in backlog DONE block; commit on the
      feature branch; merge after green benchmark; fleet delivery via
      `tools/fleet.sh upgrade` after merge (v17 rollout — panel already
      live as the before-snapshot).

Risks: (a) grader derivation vs `stacks` rename — grade.py derives from
source, so expectations follow; only the planted legacy manifest is new;
(b) fresh-agents may not create `docs/cases/` — the Step 4 table row is
the delivery mechanism, same as every other scaffolded artifact;
(c) H2 drift recurrence (BL-025.1) — if observed, add the pin to
sdd-relevant law prose in this cycle (one line).
