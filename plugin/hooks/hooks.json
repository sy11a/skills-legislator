{
  "description": "legislator-hooks \u2014 write-guard on owned law, git conduct guard, format-on-edit, OKF-sync reminder",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'for p in python3 py python; do command -v \"$p\" >/dev/null 2>&1 && exec \"$p\" \"$1\"; done; exit 0' hookshim \"${CLAUDE_PLUGIN_ROOT}/hooks/guard_owned_files.py\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'for p in python3 py python; do command -v \"$p\" >/dev/null 2>&1 && exec \"$p\" \"$1\"; done; exit 0' hookshim \"${CLAUDE_PLUGIN_ROOT}/hooks/guard_git_conduct.py\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'for p in python3 py python; do command -v \"$p\" >/dev/null 2>&1 && exec \"$p\" \"$1\"; done; exit 0' hookshim \"${CLAUDE_PLUGIN_ROOT}/hooks/format_on_edit.py\"",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'for p in python3 py python; do command -v \"$p\" >/dev/null 2>&1 && exec \"$p\" \"$1\"; done; exit 0' hookshim \"${CLAUDE_PLUGIN_ROOT}/hooks/okf_sync_check.py\""
          }
        ]
      }
    ]
  }
}
